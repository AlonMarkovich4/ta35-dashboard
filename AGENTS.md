# AGENTS.md — כללי עבודה לפרויקט TA-35

> נטען אוטומטית ע"י Codex.
>
> **מקור האמת לארכיטקטורה ולסטטוס הוא [CLAUDE.md](CLAUDE.md).** הקובץ הזה מכיל
> רק את כללי הבטיחות שאסור לפספס גם אם לא נפתח קובץ נוסף. כל מה שמשתנה עם
> התקדמות הפרויקט — מודולים, שלבים, מה הושלם ומה הבא — נמצא שם ושם בלבד.
>
> **למה:** עד 01/08/2026 הקובץ הזה שכפל את תוכן CLAUDE.md, והשניים נפרדו.
> הוא נשאר תקוע על "מנגנון המרווח — בבנייה, שלב 3 הבא" חודש אחרי שהמנגנון
> הושלם (שלבים 1–6), ולא הכיר את לולאת הלמידה, את הגשרים, ואת לוח המסחר.
> סוכן שקרא אותו קיבל תמונה שגויה. זו בדיוק הסיבה לכלל "מקור אמת אחד" למטה.

## קרא לפני עבודה

| קובץ | מה יש בו |
|---|---|
| **[CLAUDE.md](CLAUDE.md)** | ארכיטקטורה, סטטוס המודולים, מה הושלם ומה פתוח |
| **[HANDOFF.md](HANDOFF.md)** | הקשר תפעולי שאינו בקוד: המערכת השנייה, סכמות DB, החלטות מצ'אט, גישת אבחון |
| [PROJECT_GOAL.md](PROJECT_GOAL.md) | חזון ומטרות |

## Workflow (קריטי)
- **לעולם לא `git push` בלי אישור מפורש מהמשתמש.**
- לפני כל push: הצג `git diff` + הרץ `pytest` והצג שהכל עובר.
- **גם כתיבה ל-DB דורשת אישור מפורש**, ולפניה dry-run שמראה בדיוק מה ישתנה.
- עבודה לבד → fast-forward merges, היסטוריה ליניארית.
- אל תקמט קבצים לא רלוונטיים (.DS_Store וכו').

## אבטחת מידע
- **לעולם אל תדפיס, תכתוב, או תקמט DATABASE_URL / סיסמאות / API keys** בקוד,
  בלוגים, בפלט סקריפטים, או בהודעות.
- סקריפטים מקבלים סודות ממשתני סביבה בלבד.
- אם סוד נחשף — הזהר את המשתמש מיד והמלץ rotate.

## שלמות דאטה
- **Append-only sacred:** אל תמחק/תדרוס `paper_trades`, `paper_portfolios`.
  הדאטה זהב ל-ML. מסלול ה-UPDATE היחיד הוא `close_trade`.
- **Dry-run לפני כל כתיבה ל-DB:** סקריפט אבחון קריאה-בלבד
  (`SET TRANSACTION READ ONLY`) שמאמת מול דאטה אמיתי לפני שכותבים.
- מחיקה — רק עם גיבוי באותה טרנזקציה, ורק אחרי שהמשתמש ראה בדיוק מה יימחק.
- כל עסקה נשמרת עם `market_snapshot_json` מלא.

## גבולות בעלות
- **`tase-pipeline` הוא מערכת של מתכנת שני** שיושבת על אותו Supabase.
  **לא לגעת בו מכאן.** ממצא לגביו הוא מידע להעביר, לא משימה.
  הוא הבעלים של `tase_putcall`, `iron_condor_strategies`, `condor_settled_detail`
  ו-`pipeline_state` — מהצד שלנו אלה SELECT בלבד.
- **תיקים 2–7 = benchmark מוקפא.** תיק 8 = המלצות המערכת. אל תאחד ביניהם.

## QA והנדסה
- כתוב **בדיקות לכל לוגיקה לא-טריוויאלית** (helpers טהורים → unit tests).
- **תקן שורש, לא תסמין** — תיקון בפונקציה מגן על כל הקוראים, כולל אוטומציה עתידית.
- **pytest לא בודק רינדור UI** (הלקח של Styler.applymap) — אמת חזות בדפדפן אחרי deploy.
- שגיאות שנבלעות מסוכנות — תמיד logging לפני `return None`/`except`.
- **העדף מצב נגזר (compute) על מצב מאוחסן (store) — מקור אמת אחד.**

## שני לקחים מאירוע תשעה באב (23/07/2026)

הרחבה מלאה ב-HANDOFF.md סעיף 8. בקצרה, כי הם חלים על כל שינוי:

1. **הצלחה שקטה מסוכנת יותר מכישלון רועש.** אם מסלול לא יכול להאדים — הוא לא
   מנוטר. שלושה Actions רצו ירוקים שבוע שלם בזמן שהדאטה היה קפוא.
2. **רשומה ברשימת "ידוע/מוכר" חייבת סיבה, תאריך, ודיווח בכל ריצה.** אחרת היא
   לא תיעוד — היא הסתרה. שש עסקאות ישבו פתוחות שישה שבועות מאחורי רשומה כזו,
   שהסיבה שנרשמה בה הייתה גם שגויה.

## סביבה
- repo: `ta35-dashboard`. נתיב מקומי: `~/Projects/ta35-dashboard/ta35-dashboard/`
  (הועבר מ-`~/Desktop` בגלל חסימות TCC של macOS).
- DB דרך pooler: `aws-1-ap-southeast-2.pooler.supabase.com:5432`.
  העדף חיבורים קצרים; אל תחזיק טרנזקציה פתוחה.
- `DATABASE_URL` יושב ב-`web/.env.local` (ב-`.gitignore`). הזרק inline —
  ראה HANDOFF.md סעיף 5.
- Render deploy מ-`main` (~3 דק').
- ה-Actions רצים **שני–שישי** (`cron '... 1-5'`) — הבורסה עברה לשבוע ב'–ו' ב-2026.
