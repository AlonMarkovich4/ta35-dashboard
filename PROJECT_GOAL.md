# 🎯 PROJECT GOAL — TA-35 Expiry Intelligence

## החזון
מערכת אוטונומית לסחר (paper) באופציות על מדד ת"א-35, סביב פקיעות. המערכת:
1. **מחליטה** — בוחרת אילו אסטרטגיות לפתוח על סמך ניתוח, לא בעיוור.
2. **מסבירה** — לכל החלטה יש נימוק מבוסס-נתונים ("פתחתי X כי...").
3. **מתעדת** — כל עסקה נשמרת עם ההקשר המלא (append-only).
4. **לומדת** — התוצאות מזינות חזרה ומשפרות את ההחלטות הבאות.

המטרה הסופית: מערכת שסוחרת לבד ומקבלת החלטות איכותיות, ומשתפרת עם הזמן.

## המגבלה האמיתית של הדאטה (כדי לא לרמות את עצמנו)
- `expiry_history`: 965 פקיעות (2010–2026) — אבל רק **תנועת מדד** (move_pct),
  ללא מחירי אופציות היסטוריים.
- שרשראות אופציות אמיתיות קיימות רק ל-~9 פקיעות אחרונות.
- P&L אמיתי בשקלים קיים רק ל-~11 פקיעות (iron_condor_strategies, רק IC).
- **המסקנה:** אי-אפשר לעשות backtesting של P&L אמיתי על ההיסטוריה. אפשר
  backtesting הסתברותי (האם תנאי הניצחון התקיים) על כל 965 הפקיעות.

## המסלול שנבחר: A + C
- **מסלול A (עכשיו):** מנוע החלטה הסתברותי על 965 הפקיעות. נשען על
  `backtester.py` הקיים (win_rate לכל אסטרטגיה לפי move_pct).
- **מסלול C (במקביל, לעתיד):** כל עסקה שנפתחת שומרת את שרשרת האופציות המלאה
  ב-market_snapshot_json. כך נצבור קדימה את הדאטה שחסר מהעבר (מחירי אופציות +
  תוצאות אמיתיות), ובעוד מספר חודשים יהיה מדגם אמיתי ל-ML מבוסס-P&L.

## מנוע ההחלטה — בשכבות (כל שכבה מוכחת לפני הבאה)
- **שכבה 1 (בבנייה):** החלטה על בסיס **סוג פקיעה (W/M) + תנודתיות + ה-backtester
  ההיסטורי**. כל הדאטה קיים, הכל ניתן לבדיקה, וההחלטה מסבירה את עצמה.
- **שכבה 2 (עתידי):** הוספת **ניתוח חדשות live** כמשקל מתקן — בזהירות, על בסיס
  דאטה שייצבר. כרגע יש רק טבלת events קטנה (22 שורות) — לא מספיק להחלטות.

## עקרונות-על (לא ניתנים למשא ומתן)
- **Append-only sacred** — paper_trades / paper_portfolios לעולם לא נמחקים או
  נדרסים. הדאטה הוא זהב ל-ML עתידי.
- **לבנות ולבדוק ידנית לפני אוטומציה** — תפסנו ככה כבר באג יתרה ובאג connections.
- **לתקן שורש, לא תסמין.**
- **Single source of truth** — מצב נגזר (compute) בטוח ממצב מאוחסן (store).

## ארכיטקטורה (מצב נוכחי)
- Frontend: Streamlit על Render (`ta35-dashboard`).
- Pipeline חיצוני: `tase-pipeline` (Render worker) — מושך נתונים ל-Supabase.
- DB: Supabase Postgres (חיבור דרך pooler).
- שתי מערכות כותבות לאותו DB — שלנו (paper_*, expiry_history, events) ו-pipeline
  החיצוני (tase_putcall*, iron_condor_strategies, demo_*, pipeline_state).
