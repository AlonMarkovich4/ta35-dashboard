# HANDOFF — מה שלא נמצא ב-repo

> **עודכן 07/08/2026.**
>
> המסמך מכיל שלושה סוגי מידע:
> 1. **פנימיות המערכת השנייה, סכמות DB, והחלטות מצ'אט** — נאספו בסשן של 27/07
>    מול מקורות חיים. עדיין תקפים; מסומן היכן השתנה.
> 2. **תקלות שטופלו** — תשעה באב ובאג ה-benchmark (סעיף 3), ובאג הסילוק (סעיף 11).
> 3. **הבעיה הפתוחה הגדולה** — מחיר המילוי (סעיף 11). `bid`/`ask` אינם קיימים במקור
>    ונסגרו כשאלה; VWAP הוא מחיר עסקה אמיתי וזמין — אבל רק ל-~20% מהסטרייקים.
> 4. **למה המערכת מפסידה** — נענה 11/08 בסעיף **12**. בקצרה: hold בפועל נמוך
>    מהנדרש בכל רוחב רצועה, והשוק נשחק 26 נקודות ב-10 שנים. אם אתה קורא
>    את המסמך כדי להבין למה שום דבר לא עובד — **התחל שם**.
> 5. **נזילות וביצוע** — סעיף **13**. ספר פקודות אינו קיים ולא יהיה; מחזור
>    יומי כן; עקומה תוך-יומית נאספת מ-11/08.
>
> איפה שלא הצלחנו לאמת כתוב **לא ידוע** במפורש. אל תמלא את החורים בניחוש.
>
> 🔴 **קרא את סעיף 11 לפני שאתה מסתמך על מספר P&L כלשהו במסמך הזה.** ב-06/08/2026
> התגלה שכל העסקאות נסגרו במחיר סילוק שגוי. הן תוקנו, וסעיפים 0, 8 ו-9 עודכנו —
> אבל **מסקנות שנשענו על P&L לפני התאריך הזה בטלות**, וגם אחרי התיקון מחירי
> ה**כניסה** עדיין אינם סחירים.

---

## 0. מצב נוכחי — 11/08/2026 (שלישי)

### הנתונים

| | |
|---|---|
| `expiry_history` | **993** שורות · 25/02/2010 → 31/07/2026 · מתוכן **28 משוחזרות** |
| `tase_putcall_history` | 14,604 שורות · 22/05 → 10/08 · **נכתב ~17:15 בלבד** (ראה 11.7) |
| `tase_putcall` | הצילום החי, מתעדכן כל ~15 דק' — **זה המקור לפתיחת עסקאות** |
| `paper_trades` | **231** — 200 סגורות · 31 פתוחות |
| `margin_recommendations` | 81, אחרונה 11/08 |
| `decision_log` | 114 |
| `expiry_history_backup_20260731` | 27 שורות — גיבוי השורות שנמחקו |
| `paper_trades_backup_reprice_20260806_153311` | 187 שורות — גיבוי לפני תיקון הסילוק |

### התיקים

| id | תיק | תמחור | הערה |
|---|---|---|---|
| 2–7 | benchmark, שש אסטרטגיות | `lastrate` | **מוקפא.** אל תאחד עם 8/9 |
| 8 | המלצות המערכת — Iron Condor · 100k | `lastrate` | **חזר לסחור 11/08** אחרי 12 יום שבת |
| **9** | המלצות המערכת — תמחור VWAP · 100k | **מחיר עסקה** | נבנה 07/08 · סעיף **11.6** |
| **10** | **סינון נזילות · 20k** | **מחיר עסקה + עומק** | נבנה 11/08 · סעיף **13.5** |

שלושת התיקים מריצים את **אותן המלצות** ונבדלים במשתנה אחד כל פעם:
8↔9 = כמה מהתוצאה של תיק 8 היה מחיר מומצא · 9↔10 = מה עולה לדרוש עומק
מסחר. כולם רצים ב-09:00 UTC מאותו snapshot. פירוט: **13.6**.
**אל תשנה פרמטרים באמצע** — כל שינוי מאפס את ההשוואה.

> ⚠️ **תיק 8 לא פתח עסקה בין 30/07 ל-11/08 — 22 פקיעות שהוחמצו.** הסיבה:
> `RECO_TRADING_ENABLED` כובה ב-27/07 ולא הוחזר אחרי שהשערים נבנו. הפער הזה
> קבוע ואינו ניתן לשחזור — אין המלצות שמורות לרוב הימים האלה.

### Track record — 11/08/2026 (אחרי תיקון הסילוק)

**ה-ROC הוא העמודה שקובעת, לא ה-P&L.** ההון שנפרס נבדל פי ~15 בין האסטרטגיות
(סעיף 9.2), ולכן השוואת שקלים בין שורות אינה תקפה.

| אסטרטגיה | עסקאות | Win | P&L | ROC |
|---|---|---|---|---|
| Long Straddle | 31 | 48% | ‎-1,375 | ‎-1.0% |
| Long Put Butterfly | 31 | 26% | ‎-721 | ‎-7.2% |
| Short Iron Condor | 31 | 58% | ‎-3,102 | ‎-7.2% |
| Long Strangle | 31 | 32% | ‎-5,049 | ‎-8.1% |
| Long Call Butterfly | 31 | 26% | ‎-881 | ‎-8.6% |
| Bull Call Spread | 31 | 35% | ‎-4,268 | ‎-20.5% |
| **סה"כ benchmark** | 186 | | **‎-15,396 ₪** | |
| תיק 8 (המלצות) | 13 | 54% | ‎-2,535 ₪ | **‎-17.5%** |
| תיק 9 (VWAP) | 1 | 100% | +252 ₪ | +20.5% ⚠️ n=1 |
| תיק 10 (נזילות, 20k) | 0 סגורות | — | — | נפתח 11/08 |

**אין יותר אף אסטרטגיה חיובית.** Long Straddle היה +1.3% ב-07/08 והתהפך
ל-‎-1.0% אחרי פקיעת 11/08. אל תצטט את +20.5% של תיק 9 — הוא עסקה אחת.

⚠️ **כל שורות ה-benchmark ותיק 8 ממולאות ב-`lastrate`** — מחיר שאינו מחיר
עסקה (11.2). תיק 9 הוא היחיד שממולא במחיר אמיתי.

**ולמה זה קורה — התשובה נמצאה 11/08 (סעיף 12):** hold בפועל נמוך מה-hold
הנדרש **בכל רוחב רצועה**, והפער השתנה מ-‎+7.7 נקודות ב-2016–2018 ל-‎-19 היום.
זו לא תקלה במערכת אלא שוק שהשתנה.

> ⚠️ **המספרים האלה שונים מכל מה שנרשם כאן לפני 06/08.** תיקון הסילוק שינה
> 187 עסקאות ו-**43 מהן הפכו סימן (23%)**. תיק 8 עבר מ-**+150 ל-‎-2,535** —
> כלומר "המערכת התחילה להרוויח" היה ארטיפקט מדידה, לא ביצוע.
>
> ההיגיון מכני ולא מקרי: סילוק בפתיחה (הנכון) במקום בנעילה הקודמת (השגוי)
> מכניס את **פער הלילה** למשוואה. פער הוא תנועה, ותנועה פוגעת במוכרי תנודתיות
> ועוזרת לקוניה. לכן הקונדור החמיר והסטרדל השתפר. פירוט: סעיף 11.

**Short Iron Condor — 55% Win Rate ו-‎-11.0% ROC.** מחזיקה ביותר ממחצית המקרים
ומפסידה, כי השבירה עולה יותר מהפרמיה. זו האסטרטגיה שהמנוע ממליץ עליה.

**Long Straddle הוא כרגע היחיד עם ROC חיובי**, ו-+2.2% הוא בתוך הרעש.

**פתוחות (25):** 24 לפקיעות 07–13/08, ו-**`id=36`** — היתומה המוכרת
(פקיעה 23/07, תשעה באב). רשומה ב-`_ACKNOWLEDGED_ORPHANS` וב-`KNOWN_UNSETTLED`.

> ✅ **`opened_at` תוקן 02/08.** השחזור סימן את כל 132 העסקאות כנפתחו ב-02/08,
> כי `open_trades_for_expiry` קובע `datetime.now()`. ה-P&L היה תקין לאורך כל
> הדרך (מחיר הכניסה מהשרשרת ההיסטורית הנכונה), אבל `expiry_date − opened_at`
> יצא שלילי עד ‎-40 ימים.
>
> **איך נקבע התאריך — אימות, לא הסקה:** לכל עסקה שמור
> `market_snapshot_json->>'atm_level'` מרגע הכניסה. הושווה ל-ATM של השרשרת
> ההיסטורית ביום המועמד; התאמה <0.05 = אומת. **132/132 אומתו, אפס דילוגים.**
> השעה נלקחה מ-`fetch_time` של אותו snapshot (למשל 16/06 17:17).
> גיבוי: `paper_trades_opened_at_backup_20260802`.
>
> ⚠️ **`days_to_expiry` בתוך `market_snapshot_json` עדיין שגוי** (למשל ‎-40)
> באותן 132 שורות. לא תוקן **בכוונה**: ה-snapshot אמור לשקף "מה ראינו ברגע
> הכניסה", ועריכה שלו בדיעבד הופכת אותו לפחות אמין. אל תסתמך על השדה הזה
> בעסקאות משוחזרות — חשב מ-`expiry_date − opened_at`.

**תקופת החזקה בפועל** (מדידה רק אחרי התיקון): benchmark ממוצע **5.7** ימי לוח
(1–7), תיק 8 ממוצע **5.1** (1–7).

### המנוע

`regime=calm` · `recent_move=0.612%` · מדגם מותנה **315** · מרווח נבחר **1.75%**

### מתגים

| דגל | מצב | תנאי להדלקה / הערה |
|---|---|---|
| `RECO_TRADING_ENABLED` | **true** (מ-11/08/2026) | הודלק אחרי אימות בקוד ששלושת השערים סגורים — ראה 11.7 |
| `HISTORY_UPDATER_ENABLED` | **false** (ברירת מחדל) | אחרי שבאג הסילוק ב-`tase-pipeline` יתוקן |
| `BENCHMARK_TRADING_ENABLED` | **true** | תיקים 2–7. אין תלות במנוע |
| `VWAP_TRADING_ENABLED` | **true** | תיק 9 (11.6). **נפרד מ-`RECO_TRADING_ENABLED`** בכוונה — הדלקת אחד אינה מדליקה את השני |
| `LIQUIDITY_TRADING_ENABLED` | **true** | תיק 10 (13.5) |

---

## 1. אוסף הדאטה — מי כותב ל-`tase_putcall`

### 1.1 בעלות — קרא את זה קודם

**`tase-pipeline` הוא מערכת נפרדת של מתכנת שני**, שיושבת על אותו Supabase.
**לא לגעת בו מכאן.** כל ממצא לגביו הוא מידע להעביר, לא משימה.

repo: `github.com/AlonMarkovich4/tase-pipeline` — ציבורי, נוצר 20/05/2026,
push אחרון 30/06/2026. **אין בו GitHub Actions** (`gh run list --repo ...` ריק).

לא n8n, לא Make, לא cron על המק. אומת: `crontab -l` ריק, `~/Library/LaunchAgents/`
מכיל רק Google/Homebrew.

קבצי מפתח: `main.py`, `tase_api.py`, `browser.py` (Playwright), `database.py`,
`option_schema.py` (שער איכות נתונים), `strategy_engine.py`, `telegram_bot.py`,
`config.py`, `Dockerfile`, `health_server.py`, `supabase_setup.sql`.

### 1.2 איך זה רץ

**Docker → Render Background Worker**, auto-deploy מ-`main`.
`health_server.py` חושף `GET /` על `PORT` (ברירת מחדל 10000) ל-liveness,
ומחזיר 503 אחרי 5 כשלונות רצופים.

**שם ה-service ב-Render וה-ID — לא ידוע.** אין גישה ל-Render מכאן.

### 1.3 תדירות ושעות

מ-`config.py`:

```
TRADING_DAYS = {0,1,2,3,4}      # שני–שישי
MARKET_OPEN  = 09:30            # שעון ישראל
MARKET_CLOSE = 17:30
FETCH_INTERVAL_MINUTES = 15
BROWSER_RESTART_SECONDS = 6h
```

> ✅ **אי-ההתאמה שהייתה כאן — תוקנה 31/07/2026.** ה-Actions של `ta35-dashboard` רצו
> על `cron '... 0-4'` (ראשון–חמישי) בעוד הבורסה עברה ל-שני–שישי בתחילת 2026. כלומר
> רצו בימי ראשון בלי דאטה, ודילגו על שישי — יום מסחר מלא עם פקיעות. תוקן ל-`1-5`.
> אומת משני מקורות: `expiry_history` (אפס פקיעות ביום ראשון ב-2026) ו-Yahoo TA35.TA.

### 1.4 מאיפה הוא מושך

Playwright Chromium headless, בקשות מתוך ה-origin של tase.co.il דרך `page.evaluate`
(עוקף WAF):

| מטרה | endpoint |
|---|---|
| רשימת פקיעות | `GET https://api.tase.co.il/api/derivatives/fltrputvscallexpdates?objId=01&lang=0&dType=2&date=` |
| שרשרת Call/Put | `POST https://api.tase.co.il/api/derivatives/putvscall` |

3 ניסיונות לעמוד עם backoff (5/10/20 שניות), המתנה 3–5 שניות בין פקיעות.

**מדד TA-35 עצמו לא מגיע מ-TASE** — `UnderlingAsset` חוזר ריק. המדד ומחיר הסילוק
מגיעים מ-Yahoo: `https://query1.finance.yahoo.com/v8/finance/chart/TA35.TA`.

### 1.5 הכתיבה ל-Supabase

**דרך PostgREST, לא דרך `DATABASE_URL`.** `supabase_client.py` דורש `SUPABASE_URL`
+ `SUPABASE_KEY` — **לא קיימים בפרויקט הזה**, רק ב-env של Render.

- upsert עם `on_conflict=fetch_date,fetch_time,expiry_date,derivativeid_call,derivativeid_put`
- אצוות של 50 שורות
- `SUPABASE_TABLE` (ברירת מחדל `tase_putcall`), `SUPABASE_HISTORY_TABLE` (`tase_putcall_history`)

### 1.6 סמנטיקה של העמודות

| עמודה | משמעות |
|---|---|
| `fetch_date` | תאריך **שעון ישראל** של מחזור האיסוף, `TEXT` `YYYY-MM-DD` |
| `fetch_time` | שעת המחזור שעון ישראל, `TEXT` `HH:MM` |
| `trade_date` | `TradeDate` מה-payload, `TEXT` בפורמט **`DD/MM/YYYY`** (שונה!). יום המסחר שהנתונים מייצגים, בדרך כלל **T-1** |
| `fetched_at` | `timestamptz DEFAULT now()` — זמן ה-INSERT ב-UTC, **לא** זמן האיסוף |
| `drvtype` | pass-through. בפועל תמיד `'04'`. מה זה מייצג — **לא ידוע** |
| `rowtype` | pass-through. בפועל `NULL` תמיד |

מיפוי `key.lower()` מול whitelist `VALID_COLUMNS` — כל שדה שלא ברשימה נזרק בשקט.

### 1.7 מחזור החיים של הטבלה

`tase_putcall` מחזיקה **snapshot אחד בלבד**:

1. upsert לכל הפקיעות.
2. אם **כל** הפקיעות הצליחו → `_clear_old_snapshots()` מוחק כל מה שאינו
   ה-`(fetch_date, fetch_time)` הנוכחי.
3. אם רק **חלק** הצליחו → הניקוי **מדולג בכוונה**, כדי לא לאבד snapshot שלם.
4. במחזור האחרון של היום → `copy_to_history()` מעתיק ל-`tase_putcall_history`,
   מוגן ב-`pipeline_state` תחת `history_copied:<date>`.

**ההיסטוריה היחידה של שרשרות היא `tase_putcall_history`** — snapshot אחד ליום.

### 1.8 מה קורה כשאין מסחר — קריטי להבנה

**א. פקיעה בלי עסקאות** (`items` ריק): `upsert_no_trading()` כותב שורת placeholder
עם `derivativename_* = 'ללא מסחר'`. **בפועל מעולם לא קרה.**

**ב. ה-feed לא התקדם** (`STALE_TRADE_DATE`) — **זה מה שקרה בתשעה באב**:
`option_schema.check_trade_date()` סופר ימי מסחר בין `trade_date` ל-`fetch_date`.
- פער של יום מסחר אחד = T-1 = תקין.
- פער של **2 ומעלה** → `DQLevel.CRITICAL` → **ה-upsert מדולג לחלוטין.**
- התראת Telegram אחת לכל קוד ליום (`dq_alert:<CODE>:<date>`).
- ה-transport נחשב תקין → אין restart ואין crash alert.

מגבלה מתועדת ב-`option_schema.py` עצמו: **אין לוח שנה של חגים.** `TRADING_DAYS`
הוא ימי שבוע בלבד, ולכן היום הראשון אחרי חג באמצע שבוע סופר יום מסחר עודף
ומדליק `STALE_TRADE_DATE`. **בחירה מודעת** — "fail-safe, חוסם במקום לסחור על דאטה ישן".

> 💡 זה בדיוק מה שקרה: האוסף **לא נכשל**. הוא זיהה נכון שאין נתונים חדשים ועצר.
> הבעיה הייתה אצלנו — שלא ידענו לשאול אם בכלל היינו אמורים לקבל משהו.

### 1.9 לוגים

- **לוגים אמיתיים: Render service logs בלבד.** אין טבלת לוגים ב-DB. לא נגישים מכאן.
- **התראות Telegram** — token ו-chat id ב-env של Render.
- **העקבות היחידות שכן נגישות: `pipeline_state`.** זו הדרך המעשית לבדוק מה קרה:

| מפתח | שעה (UTC) | משמעות |
|---|---|---|
| `dq_alert:<CODE>:<date>` | ~06:30 | התראת איכות-נתונים |
| `settlement_done:<date>` | ~07:01 | סילוק הפקיעה של אותו יום |
| `weekly_heartbeat:<YYYY>-W<ww>` | ~07:01 שני | heartbeat שבועי |
| `strategy_triggered:<YYYY>-W<ww>` | ~09:05 שני | יצירת אסטרטגיות לשבוע |
| `daily_summary_sent:<date>` | ~14:20 | סיכום יומי ב-Telegram |
| `history_copied:<date>` | מיד אחריו | העתקה לארכיון |

---

## 2. סכמות DB

מקור: `information_schema` + `pg_indexes`, נקרא 27/07. **שים לב לסוגי האובייקטים** —
חלק Views:

| אובייקט | סוג |
|---|---|
| `tase_putcall`, `tase_putcall_history` | BASE TABLE |
| `iron_condor_strategies` | BASE TABLE |
| **`condor_settled_detail`** | **VIEW** |
| `best_condor_per_expiry`, `condor_weekly_potential` | VIEW |
| `expiry_history`, `paper_trades`, `paper_portfolios` | BASE TABLE |
| `decision_log`, `margin_recommendations` | BASE TABLE |
| `pipeline_state`, `events`, `demo_trades`, `demo_balance` | BASE TABLE |

### `expiry_history`

```
expiry_date TEXT, expiry_time TEXT, expiry_type TEXT,
base_price DOUBLE PRECISION, expiry_price DOUBLE PRECISION,
open_pct, close_price, daily_pct, volume, transactions, points,
abs_move_pct, move_pct  (כולם DOUBLE PRECISION / BIGINT)
```

**אין PK, אין אינדקסים, אין constraints — אפס.** מניעת כפילויות ברמת האפליקציה בלבד.

- `close_price IS NULL` **מבודד בדיוק את השורות המשוחזרות** (28 כרגע). זה סמן המקור היחיד.
- `base_price[i] == close_price[i-1]` בשורות ה-CSV — אומת 12/12. כלומר **תנועת מושב אחד**.

### `condor_settled_detail` (VIEW)

```sql
SELECT ... FROM iron_condor_strategies
WHERE is_valid = true AND actual_pnl_ils IS NOT NULL
```

> ⚠️ **מלכודת שעלתה בדם.** `is_valid=false` מפיל שורות מה-VIEW, ו-`auto_close_expiries`
> רואה **רק** את ה-VIEW. פקיעת 19/06 הייתה עם סטלמנט תקין אבל כל 8 שורותיה
> `is_valid=false` → 6 עסקאות benchmark נשארו פתוחות שישה שבועות בשקט.
> `is_valid=false` אינו נדיר: ~60 שורות מול ~212 `true`.

### `paper_trades`

```
id BIGSERIAL PK, portfolio_id FK, strategy_id INT, strategy_name TEXT,
expiry_date DATE, opened_at, entry_index, entry_cost, legs_json JSONB,
max_profit, max_loss, status TEXT DEFAULT 'open', closed_at, close_index,
pnl, pnl_pct, market_snapshot_json JSONB, num_legs,
entry_commission, exit_commission

UNIQUE idx_trades_unique (portfolio_id, strategy_id, expiry_date)   ← הדדופ האמיתי
```

**אין status של cancelled/void.** מסלול ה-UPDATE היחיד הוא `close_trade`.

### `paper_portfolios`

| id | שם | strategy_ids |
|---|---|---|
| 2–7 | ששת האסטרטגיות | `[1]`…`[6]` — **benchmark קבוע, אל תיגע** |
| 8 | המלצות המערכת — Iron Condor | `[102]` |

כולם `initial_balance = current_balance = 100,000`. `current_balance` לא מתעדכן —
היתרה נגזרת מ-`compute_balance`.

### `decision_log` / `margin_recommendations`

**אין UNIQUE בשתיהן** — הדדופ בקוד בלבד (`*_logged_today`).
**נוצרו ידנית ב-Supabase SQL Editor**, לא במיגרציה. **אין מנגנון מיגרציות בפרויקט.**

⚠️ `engine_version`: **`margin-v1` שגוי. רק `margin-v1.1` תקף.** אין constraint שאוכף.

### מאיפה מגיע `actual_index_close`

```
Yahoo TA35.TA meta.regularMarketOpen
  → tase-pipeline/strategy_engine.py::_fetch_settlement_price()
    → settle_expiry(today_iso)   [רץ אחרי 10:00, רק על הפקיעה של היום]
      → iron_condor_strategies.actual_index_close
        → VIEW condor_settled_detail → ta35-dashboard
```

> 🔴 **אומת — לא עוד חשד.** `actual_index_close` הוא **סגירת המושב הקודם**, לא פתיחת
> יום הפקיעה כפי שהקוד מתכוון. הושווה מול נרות Yahoo על **27 פקיעות: 27/27 התאמה
> מדויקת** לסגירת היום הקודם, אפס התאמות לפתיחת יום הפקיעה.
>
> החשוד: `settle_expiry` רץ ~10:00 שעון ישראל, ובאותו רגע `meta.regularMarketOpen`
> עדיין מציג את המחזור הקודם.
>
> **ההשלכה רחבה מ-`expiry_history`:** `iron_condor_strategies.actual_pnl_ils` מחושב
> מהמחיר הזה, וכך גם `close_index` של עסקאות שנסגרו אוטומטית. כלומר גם ה-P&L מוסט.
>
> **זה ב-`tase-pipeline`, לא כאן.** להעביר למתכנת השני כראיה לבדיקה. הוא שיפר
> לאחרונה את הטיפול בתאריכים — ייתכן שכבר בטיפול; המדידה שלנו היא עד 31/07 אחורה.

> ✅ **טופל אצלנו 06/08/2026 — אבל רק אחרי שבוע שבו האבחנה הזו הייתה כתובה ולא פעלנו.**
>
> המשפט "זה ב-`tase-pipeline`, לא כאן" נכון לגבי **המקור** ומטעה לגבי **הצריכה**.
> אנחנו קראנו את הערך הזה כמחיר סילוק בשני מסלולים משלנו, וזה היה בשליטתנו כל הזמן.
> מ-06/08 `paper_db` קורא מ-`expiry_history.expiry_price` (עם גיבוי לפתיחת המדד),
> ובדיקות רגרסיה נועלות את השם `condor_settled_detail` מחוץ לשני המסלולים.
>
> **הלקח:** ממצא על מערכת של מישהו אחר עדיין מחייב לשאול "ומה **אנחנו** עושים איתו
> בינתיים". "לא הבאג שלנו" אינו תשובה תפעולית. ראה סעיף 11.
>
> ההעברה למתכנת השני **עדיין פתוחה** — כל צרכן אחר של הטבלה סובל מאותה שגיאה.

---

## 3. תקלות שטופלו (3.1–3.5 = תשעה באב · 3.6 = תיקי ה-benchmark)

### 3.1 מה קרה (23–26/07)

| תאריך | `dq_alert` | שורות | `settlement_done` |
|---|---|---|---|
| 22/07 | — | 259 (`trade_date=21/07`) | ✅ |
| **23/07** (תשעה באב) | **`STALE_TRADE_DATE`** | **0** | ❌ |
| **24/07** | **`STALE_TRADE_DATE`** | **0** | ❌ |
| 25–26/07 | — | — | שבת + ראשון, לא ימי מסחר |
| 27/07 09:30 | — | 352 ✅ | התאושש לבד |

ב-23/07 הבורסה לא נפתחה → TASE לא פרסמה קובץ → הפער הגיע ל-2 ימי מסחר →
כל ה-upserts דולגו. ב-24/07 הפער גדל עוד → דולג שוב.

> **24/07 היה יום מסחר מלא** (Yahoo מראה נר). החסימה באותו יום הייתה תופעת לוואי
> של ספירת ימי המסחר בלי לוח חגים — לא סגירת בורסה.

**מה הדשבורד עשה בינתיים:** כל ה-Actions ירוקים, אף אחד לא בדק טריות. נרשמו
החלטה והמלצת מרווח לפקיעה `2026-07-23` — ביום שהבורסה הייתה סגורה.

### 3.2 מה תוקן (31/07–02/08)

| קומיט | מה |
|---|---|
| `24b25db` | כיבוי `RECO_TRADING_ENABLED` + גידור הזרמת `expiry_history` |
| `8813b46` | שער העוגן החודשי ב-`history_updater` + `scripts/backfill_expiry_history.py` |
| `3b1f395` | `src/trading_calendar.py` + שער יום-מסחר + תיקון ה-cron |
| `7fe77b9` | חיווט שער הטריות ב-`margin_recorder` |
| `2448b0b` | `_report_orphans` — גילוי עסקאות יתומות |
| `e190de5` · `778c80a` · `f2e4441` | עדכון HANDOFF / AGENTS / PROJECT_WORKPLAN |
| `a27930b` | **אוטומציה לתיקי ה-benchmark** (ראה 3.6) |
| `c4ef2ac` | `scripts/backfill_benchmark_trades.py` |
| `defb4c4` | עדכון HANDOFF |
| `a490b9a` | **hold לפי אופק זמן** (ראה סעיף 7) |
| `5d2ef77` | **ספירת האופק מ-trade_date (T-1)** (ראה 7.3) |

### 3.3 זיהום `expiry_history` — נמחק ושוחזר

**הבעיה:** `history_updater` חישב `move_pct` מ-`condor_settled_detail.base_index_value`,
שהוא עוגן של **סדרה חודשית** — אותו ערך חוזר עד 4 פקיעות שבועיות רצופות.

**המדידה:** 27 שורות (2.7% מהטבלה) שינו את הפלט מקצה לקצה:

| | עם המזוהמות | בלעדיהן |
|---|---|---|
| `recent_move` | **-4.737%** | 0.568% |
| `regime` | volatile | calm |
| מדגם מותנה | **0** | 333 |
| מרווח נבחר | 2.00% | 1.75% |

**הפתרון:** נמחקו (גיבוי: `expiry_history_backup_20260731`) ושוחזרו לפי ההגדרה
הנכונה — `base` = סגירת המושב הקודם, `expiry_price` = פתיחת יום הפקיעה מ-Yahoo.
שיטת השחזור אומתה **6/6 עד האגורה** מול הסילוק הרשמי שב-CSV, בימי החפיפה 30/04–07/05.

### 3.4 עסקאות יתומות — טופלו

**19/06 (6 עסקאות benchmark):** הסטלמנט היה קיים (4182.02) אבל `is_valid=false`.
נסגרו ידנית לפי **4173.07** (הסילוק האמיתי; 4182.02 הוא סגירת 18/06 — באג T-1).
סה"כ P&L **6,487.00 ₪**, זהה ל-dry-run.

> ⚠️ ב-`daily_review.py` היה רשום `KNOWN_UNSETTLED = {"2026-06-19"}` עם ההערה
> "פקיעה ידועה ללא סטלמנט". **ההערה הייתה שגויה** — הסטלמנט היה קיים.
> התגובה לאירוע הראשון הייתה להשתיק את ההתראה על בסיס אבחון לא נכון.

**23/07 (עסקה 36):** נשארת פתוחה. אין סילוק ולא יהיה. רשומה כ**יתומה מוכרת** —
מדווחת בכל ריצה עם הסיבה המלאה, אך אינה מפילה את הריצה.

### 3.5 השערים החדשים

שלושת המצבים שנראו זהים (וכולם ירוקים) כבר לא זהים:

| מצב | לפני | אחרי |
|---|---|---|
| לא יום מסחר | ירוק | `_trading_day_guard` מדלג עם סיבה |
| יום מסחר + דאטה טרי | ירוק | ירוק |
| **יום מסחר + דאטה ישן** | **ירוק** | **`stale_chain` → exit≠0, אדום** |
| **עסקה שלא נסגרה** | **ירוק** | **`_report_orphans` → exit≠0** |

**הנקודה המבנית:** ביום שאינו יום מסחר הרשם כלל לא מגיע לבדיקת הטריות — השער
חוסם קודם. לכן שרשרת ישנה שם **אינה יכולה** להיות "בגלל חג". זו הבחנה מבנית,
לא הנחה. `FORCE_RUN=true` עוקף להרצה ידנית.

`auto_close_expiries` **בכוונה בלי שער יום-מסחר** — הטריגר שלו הוא קיום הסטלמנט,
לא הזמן. חסימה הייתה מוסיפה מצב כשל (סטלמנט מאוחר שלא ייסגר לעולם).

`decision_recorder` **בכוונה בלי שער טריות** — נשען על השרשרת רק לזיהוי W/M, שלא מתיישן.

---

### 3.6 באג נפרד — תיקי ה-benchmark מעולם לא שוגרו (טופל 01–02/08/2026)

**לא קשור לתשעה באב.** רץ בשקט מאז 15/06/2026.

**הממצא:** `open_trades_for_expiry` — הפונקציה היחידה שפותחת עסקאות לשש
האסטרטגיות (תיקים 2–7) — נקראה ממקום אחד בלבד בכל הפרויקט מלבד טסטים:
`pages/5_paper_trading.py:803`, ממשק **Streamlit שאינו פרוס**
(`render.yaml` מרים רק Next.js + webhook). שלושת ה-Actions לא נגעו בו.

כלומר: **מעולם לא הייתה אוטומציה לתיקים האלה.** 24 עסקאות נפתחו ידנית
ב-15/06 בלחיצת כפתור, ומאז כלום — 22 פקיעות, 132 עסקאות חסרות.

> זה לא היה באג ב-UI ולא בנתונים. פשוט לא היה מי שיפעיל את הפונקציה.

**נזק משני שלא היה גלוי:** `decision_validator` משווה את החלטות המנוע מול
ה-P&L של תיקי ה-benchmark. בלי עסקאות חדשות, **19 מתוך 23 החלטות מנוע נשארו
בלי מול-מה-להשוות** — גשר 2 היה משותק שישה שבועות. זה מסביר למה ה-hit-rate
שהוצג לא אמר כלום.

**מה נעשה:**

1. `scripts/auto_open_benchmark_trades.py` + workflow — שני–שישי 09:00 UTC.
   עובר על הפקיעות הקרובות ופותח את 6 האסטרטגיות לכל אחת. דדופ ברמת ה-DB
   (`UNIQUE (portfolio_id, strategy_id, expiry_date)`) → ריצה יומית בטוחה.
   kill-switch `BENCHMARK_TRADING_ENABLED`, **ברירת מחדל דלוק** — בניגוד
   ל-RECO, כאן אין תלות במנוע: האסטרטגיות רצות על פרמטרים מוקפאים ישירות
   מהשרשרת, בלי `expiry_history` ובלי הכיול שנמצא בבדיקה.

2. `scripts/backfill_benchmark_trades.py` — שחזר **132 עסקאות** מהארכיון
   (23/06 → 31/07). לכל פקיעה נטענה השרשרת מהיום הראשון שבו הופיעה, דרך
   `open_trades_for_expiry` האמיתי. תוצאה: `open=132 · duplicate=24 · excluded=1`.
   אומת מול ה-DB: 194 עסקאות סה"כ, 30 לכל אסטרטגיה, רצף 16/06 → 07/08.

3. `supabase_loader.get_latest_option_chain` קיבל `source_table` ו-`fetch_date`
   כדי לקרוא את הארכיון דרך **אותו נתיב סינון** במקום לשכפל 60 שורות לוגיקה.
   שם הטבלה עובר whitelist (`_check_source`).

**⚠️ ההחרגה החשובה:** ה-dry-run חשף שפקיעת **2026-07-23** קיימת בארכיון עם
שרשרת מלאה, ולכן השחזור היה פותח לה 6 עסקאות — לפקיעה שאין לה סילוק ולעולם
לא יהיה. הן היו הופכות ל-6 יתומות קבועות נוספות. מוחרגת ב-`_NO_SETTLEMENT`,
עם בדיקה שמקבעת את זה. **כל שחזור עתידי חייב לשמור על ההחרגה הזו.**

**שתי טעויות שנעשו בדרך, לתיעוד:**
- הרצת `FORCE_RUN=true` על ה-DB החי כ"בדיקה" פתחה 24 עסקאות אמיתיות
  (`id 44–67`, פקיעות 04–07/08). הן נשמרו — הן בדיוק מה שהאוטומציה אמורה
  לעשות. **`FORCE_RUN` הוא להרצת אופרטור, לא לבדיקות.**
- ריצת שחזור ראשונה נכשלה בשקט על נתיבים יחסיים ב-shell רקע ולא כתבה כלום.
  מצב ה-DB אומת אחרי כל אחת מהן.

---

## 4. החלטות שסוכמו בצ'אט ולא נכתבו בקוד

מקור: תמלילי `~/.claude/projects/-Users-alonmarkovich-Projects-ta35-dashboard-ta35-dashboard/`.

### 4.1 `floor=0.97` (12/07/2026)

הסף המקורי 0.90 נדחה: בחר מרווח 1.0% ב-74% מהשבועות, 75 שבירות. נעשתה סריקת ספים
על `[0.90…0.98]`, walk-forward על 768 פקיעות, `w=0.6`.
**0.97 הוא נקודת הברך:** 23 שבירות בעשור (מול **71** ב-0.96), רצף שחור 1, פרמיה 556₪/שבוע.

`w=0.6` **נשאר כפי שהיה ולא נסרק מעולם.**

### 4.2 `wing_pct=0.75` (13/07/2026)

הרקע: המשתמש בונה בפלטפורמה שלו Iron Condor עם הגנות במרחק 20 נקודות.
סריקת כנפיים `[0.25%…2.0%]`, walk-forward על 768. בחירת המרווח זהה בכל הריצות.
**0.75% היא נקודת האיזון:** 80₪/שבוע = 82% מהשיא, max drawdown **3,712₪** מול **6,524₪**.

### 4.3 הפרדה מכוונת ב-paper trading

`strategy_payoff_params` ב-`payoff.py` נשאר על `(2.0, 1.0)` **בכוונה** —
"תיקי הדמו הם ה-benchmark. רק מנגנון ההמלצות עובר ל-0.75."
**תיקים 2–7 = benchmark מוקפא. תיק 8 = המלצות. אל תאחד.**

### 4.4 `RECO_TRADING_ENABLED` (13/07/2026)

נבנה בשלבים: kill-switch כבוי כברירת מחדל → הרצה ידנית מבוקרת → אימות שתי שאלות →
הדלקה. נוסף `min_days_to_expiry=1` אחרי שנפתחה עסקה על פקיעה של למחרת בבוקר.

> ההגנות שנחשבו מספיקות (kill-switch, דדופ, `min_days_to_expiry`, דמו בלבד)
> **לא מנעו פתיחת עסקה על שרשרת ישנה**. זו הפרצה שהתממשה ב-23/07 — ונסגרה
> ב-`7fe77b9`. הדגל כבוי כרגע.

### 4.5 ✅ חקירת "סחיפת העוגן" — **נסגרה 31/07/2026**

הסשן של 22/07 נקטע ב-API error באמצע חקירה, והשאיר סתירה פתוחה:
> *"אחד משני החישובים באג. החשוד: `hold_probability_at_margin` — אולי לא מחשב
> `P(|move| ≤ m)` כפי שהנחתי."*

**התשובה: שני החישובים תקינים. הנתונים היו מזוהמים.**

- `hold_probability_at_margin` נשען על `dist["moves"]`, שנבנה מ-**`move_pct`** —
  העמודה הנכונה (`move_distribution.py:103`). אומת בהרצה: `moves == sorted(move_pct)`.
- שוחזר `hold_blended = 0.9737` במרווח 2.25% — **זהה בדיוק** לערך שנרשם ב-DB.
- מה שהזיז את הפלט היה `recent_move` ו-`regime`, שנגזרו מ-27 השורות המזוהמות (סעיף 3.3).

**עדיין פתוח מאותה חקירה:** `recent_move` הוא קצר-זיכרון (הפקיעה האחרונה בלבד).
הוצע להחליף בחלון מתגלגל — `max|move|` של 5 האחרונות, או סטיית תקן. **לא נעשה.**

### 4.6 ⚠️ מוקש שנותר: `abs_move_pct` ≠ `|move_pct|`

בשורות ה-CSV, `abs_move_pct` (עמודת "אחוז") **אינו** `|move_pct|` — הוא נבדל
ב-962 מתוך 986 שורות. ממוצע 1.28% מול 0.49%. הוא גם אינו `|daily_pct|` (2/965)
ואינו `|open_pct|` (5/965) — **כמות רביעית שאינה מתועדת בשום מקום.**

CLAUDE.md טוען שהעמודה היא "ערך מוחלט תמיד" — **לא מדויק.**

כרגע זה **לא מזיק**: `_abs_move_series` (ב-`context_analyzer` וב-`move_distribution`)
מזין רק את סיווג ה-`regime` ואת שדה הדיווח `abs_mean`. בחירת המרווח נשענת על
`move_pct`. אבל זה מוקש — כל שימוש עתידי ב-`abs_move_pct` כאילו הוא `|move_pct|` ישגה.

---

## 5. גישה — הרצת אבחון read-only

### 5.1 היכן הסוד

`DATABASE_URL` נמצא ב-**`web/.env.local`** (ב-`.gitignore`). **הערך עצמו לא נכתב כאן.**

```bash
cd ~/Projects/ta35-dashboard/ta35-dashboard
DATABASE_URL="$(grep '^DATABASE_URL=' web/.env.local | cut -d= -f2-)" venv/bin/python <script>
```

`.streamlit/secrets.toml` ו-`.env` בשורש **לא קיימים**. זה הסוד המקומי היחיד.

חיבור דרך ה-**pooler** של Supabase (`aws-1-ap-southeast-2.pooler.supabase.com:5432`).
העדף חיבורים קצרים; אל תחזיק טרנזקציה פתוחה או `prepared statements` ארוכי-חיים.

**אין גישה לאוסף** — הוא כותב עם `SUPABASE_URL`/`SUPABASE_KEY` שיושבים ב-Render בלבד.

### 5.2 דפוס האבחון

```python
eng = create_engine(url, connect_args={"connect_timeout": 20})
with eng.connect() as conn:
    conn.execute(text("SET TRANSACTION READ ONLY"))   # ← תמיד ראשון
    ...
```

שתי ההגנות ביחד: `SET TRANSACTION READ ONLY` (השרת דוחה כתיבה) **וגם** סינון
`SELECT`/`WITH` בקוד. אל תוותר על אף אחת.

### 5.3 שאילתות פתיחה

```sql
-- האם ה-feed חי ומה גיל הנתונים  ← הבדיקה החשובה ביותר
SELECT fetch_date, fetch_time, trade_date, count(*) FROM tase_putcall GROUP BY 1,2,3;

-- מה קרה ב-pipeline (תחליף הלוגים)
SELECT key, updated_at FROM pipeline_state
WHERE updated_at >= now() - interval '3 days' ORDER BY updated_at;

-- עסקאות יתומות
SELECT id, portfolio_id, expiry_date FROM paper_trades
WHERE status='open' AND expiry_date < current_date;

-- שורות משוחזרות ב-expiry_history
SELECT count(*) FROM expiry_history WHERE close_price IS NULL;
```

### 5.4 בדיקת בריאות בלי DB

```bash
gh run list --limit 20
```

⚠️ **Actions ירוקים כבר לא מספיקים כהוכחה — אבל מ-31/07 הם כן אומרים משהו.**
לפני התיקון הכל היה ירוק גם כשהדאטה היה קפוא. עכשיו `stale_chain` ויתומה לא-מוכרת
מפילים את הריצה. הבדיקה האמיתית נשארה `max(fetch_date)` ב-`tase_putcall` מול היום.

---

## 6. מה נשאר "לא ידוע"

1. **מה TASE עשתה עם חוזה 2026-07-23** — גלגלה, סילקה בתאריך אחר, או ביטלה?
   זה הדבר היחיד שחוסם את סגירת עסקה 36. ה-snapshot של 22/07 עוד הכיל אותה
   (94 שורות, שמורות ב-`tase_putcall_history` תחת `fetch_date='2026-07-22'`);
   ב-27/07 היא נעלמה. בימים 23–24/07 האוסף היה חסום — אין לנו רישום.
2. **ערבי חג בלוח TASE** — 11/09 (ערב ר"ה) ו-25/09 (ערב סוכות). נוהג הבורסה משתנה
   בין סגירה מלאה למסחר מקוצר. **חסומים כברירת מחדל** ב-`trading_calendar`, לאימות
   מול הלוח הרשמי. 21/09 (יום כיפור) ודאי. חול-המועד סוכות הוא מסחר ואינו חסום.
3. **רשימת החגים מכוסה עד 31/12/2026 בלבד.** `holidays_are_current()` מרעישה כשתתיישן.
4. **שם ה-service וה-ID ב-Render** של האוסף, ומי מחזיק בגישה.
5. **משמעות `drvtype='04'`.**
6. **האם `w=0.6`** (משקל השילוב) נבדק אי פעם.
7. **האם באג הסילוק** (`actual_index_close`) כבר תוקן ב-`tase-pipeline` — המתכנת
   השני שיפר לאחרונה את הטיפול בתאריכים.
8. **תוכן התראות ה-Telegram** שנשלחו ב-23–24/07.

---

## 6ב. ה-Actions — מה רץ ומתי (עודכן 31/08/2026)

| Action | cron (UTC) | מה עושה | שער יום-מסחר | kill-switch |
|---|---|---|---|---|
| **`auto_trade_daily`** | `0 9` + `0 13` | **כל הצינור היומי, 5 שלבים ברצף** | ✅ בכל שלב | לכל שלב משלו (ראה למטה) |
| `auto_close_expiries` | `0 8` + `0 14` | סוגר פקיעות + מדווח יתומות | ❌ **בכוונה** | `HISTORY_UPDATER_ENABLED` (**כבוי**) |
| `archive_intraday_chain` | — **מושבת** | מארכב שרשרת תוך-יומית (13.4) | ✅ | — |

חמשת השלבים בתוך `auto_trade_daily`, לפי הסדר:

| # | סקריפט | תיק | kill-switch |
|---|---|---|---|
| 1 | `auto_record_decisions.py` | — (`decision_log`) | — |
| 2 | `auto_record_margins.py` | 8 | `RECO_TRADING_ENABLED` (**דלוק** מ-11/08) |
| 3 | `auto_open_benchmark_trades.py` | 2–7 | `BENCHMARK_TRADING_ENABLED` (דלוק) |
| 4 | `auto_open_vwap_trades.py` | 9 | `VWAP_TRADING_ENABLED` (דלוק) |
| 5 | `auto_open_vwap_trades.py --spec liquidity` | 10 | `LIQUIDITY_TRADING_ENABLED` (דלוק) |

חמשת ה-workflows הישנים עדיין קיימים כקבצים, **בלי `schedule`** — רק
`workflow_dispatch`, להרצה ידנית של שלב בודד מטאב Actions.

### למה אוחדו — 31/08/2026

**התסמין.** ב-27–28/08 ה-Actions רצו באיחור של 10–12 שעות, וב-31/08 לא רצו
כלל עד שהורצו ידנית. **60 מתוך 60 הריצות הצליחו** — לא היה כשל בקוד ולא
ב-secret. רק תזמון.

**מה נבדק ונשלל.** שתי השערות שלי נבדקו מול היסטוריית הריצות והופרכו:
(א) הרעבה מצד `archive_intraday_chain` — הריצות שלו לא התנגשו בחלון;
(ב) חוסר פעילות ב-repo — היו commits באותם ימים.
**הסיבה הנותרת:** GitHub אינו מתחייב לדיוק ב-cron ומוריד בעדיפות repo עם
הרבה טריגרים מתוזמנים. היו ~32 הפעלות ביום מ-7 workflows. **זו הסבירה
ביותר, לא מוכחת** — אין ל-GitHub API שמחזיר "נדחית כי X".

**מה נעשה.** ~32 הפעלות ביום ⟶ **4**:

* חמישה פותחים ב-09:00 → workflow אחד. גם תיקון נכונות: הם *חייבים* לצאת
  מאותו snapshot (הנימוק שכבר היה כאן), וכחמישה workflows נפרדים הם התפזרו.
* `auto_close` מ-7 ריצות ליום ל-2. הטריגר הוא קיום הסטלמנט, לא השעה.
* `archive_intraday_chain` הושבת — **לא בגלל התזמון אלא כי הוא חסר ערך**
  (13.4: הפיד T-1 וקפוא, 12 צילומים זהים ביום). היה הטריגר התדיר ביותר.

**ריצת ההשלמה ב-13:00** היא רשת ביטחון: כל שלב אידמפוטנטי (דדופ ב-DB +
שער טריות), ולכן היא no-op אם הבוקר הצליח, ומשלימה אם נדחה.

> ⚠️ **זה מקטין חשיפה, לא הוכחה.** אם גם אחרי זה הריצות יידחו — ההשערה על
> העומס שגויה, והצעד הבא הוא טריגר חיצוני (Render cron) ולא עוד כוונון cron.
> **אל תסיק שזה נפתר עד שנראה 3 ימים רצופים של ריצה בזמן.**

`continue-on-error` על כל חמשת השלבים בכוונה: כשל בשלב אחד לא יחסום את
השאר. פקיעה שלא נפתחה היום היא פער שלא ניתן לשחזר — עדיף 4 מתוך 5.

⚠️ **אבל הוא לבדו הפך את ה-job לירוק תמיד** — הוא קובע `conclusion=success`
גם לשלב שיצא בקוד שגיאה. רגרסיה שנכנסה עם האיחוד (041c5f8) והתגלתה ב-01/09:
האוסף נפל, שלבים 2 ו-3 יצאו בשגיאה **בכוונה** ("כדי שה-Action יסומן אדום"),
אפס עסקאות נפתחו — ו-`gh run list` הראה ירוק מלא.
✅ **תוקן:** לכל שלב יש `id`, ושלב סיכום אחרון בודק את `outcome` (שנשאר
`failure` תחת continue-on-error, בניגוד ל-`conclusion`) ומפיל את ה-job.
**אל תסיר את שלב הסיכום** — בלעדיו אין מסלול שבו הצינור נצבע אדום.

---

## 6ג. טריגר חיצוני — `dispatch_pipeline` (01/09/2026)

**הבעיה.** ה-`schedule` של GitHub הפסיק להיות אמין, ואי אפשר לתקן אותו מהצד
שלנו. מה שנמדד (01/09):

* **מעולם לא הייתה ריצה "בזמן"** — בסיס 25–56 דק' איחור לפחות מ-18/08.
  ב-26/08, יום שנראה תקין: cron `0 7` ירה ב-07:42, cron `0 9` ב-09:42.
* **27/08 — שינוי משטר:** 5–12 שעות איחור, **ומשבצות נופלות.**
  `auto_close_expiries` עם 7 crons: 26/08 → 7/7 · 27/08 → 1/7 · 28/08 → 1/7.
* **01/09 — אפס ריצות `schedule`.** הפקיעה של אותו בוקר לא נסגרה עד הפעלה ידנית.

**מה נשלל — אל תנסה שוב:**

| השערה | ההפרכה |
|---|---|
| תור הראנרים / concurrency | `created_at == run_started_at` בכל ריצה; הג'וב נתפס תוך 2–4 שנ'. הריצה **אינה נוצרת** |
| יותר מדי טריגרים | קוצץ מ-32 ל-4 ביום (041c5f8). למחרת: איחור 6:25. יומיים אחרי: אפס |
| הדקה `:00` עמוסה | `archive_intraday_chain` רץ על `0,30` 20 יום וקיבל אותו lag בדיוק |
| חיוב / דקות | repo ציבורי — דקות חינם ובלתי מוגבלות |

**הפתרון.** `workflow_dispatch` אינו עובר בתור המתוזמן. הראיה החזקה ביותר היא
01/09 עצמו: המתזמן ירה אפס פעמים, ו-dispatch רץ מיידית והצליח, פעמיים.

`scripts/dispatch_pipeline.py` — stdlib בלבד, בלי תלות חדשה. יורה את
`auto_close_expiries.yml` ואת `auto_trade_daily.yml`.

### ההתקנה (ידנית — לא דרך render.yaml)

1. **PAT** — fine-grained, resource owner `AlonMarkovich4`,
   **Repository access: רק `ta35-dashboard`**,
   Permissions: **Actions — Read and write** + Metadata: Read.
   ⛔ לא classic token עם scope `repo`.
2. **שירות Render מסוג Cron Job**, schedule `0 9,13 * * 1-5` (UTC),
   command: `python scripts/dispatch_pipeline.py`
3. **env var** `GH_DISPATCH_TOKEN` על השירות בלבד. לא בקובץ, לא ב-repo.

> ⚠️ **להקים דרך הדשבורד של Render, לא כ-Blueprint.** `render.yaml` מיושן —
> הוא מכריז `databases: ta35-db` ומחבר `fromDatabase`, בעוד ה-DB האמיתי הוא
> ה-pooler של Supabase. החלת הקובץ עלולה להקצות DB מיותר.

> 📅 **תאריך תפוגת ה-PAT: `[[TODO: verify]]` — למלא בהקמה.**
> זהו מצב הכשל השקט מספר 1: ה-cron ירה, ה-API החזיר 401, ואף אחד לא הסתכל.
> הסקריפט יוצא ב-exit≠0, אבל **רק אם מישהו מנטר את זה.**

### שלוש החלטות שלא לשנות בלי לקרוא

1. **אין dry-run כברירת מחדל** — היפוך מכוון מקונבנציית ה-repo. שאר הסקריפטים
   מוגנים ב-dry-run כי הם כותבים ל-DB; זה לא כותב כלום, רק מושך בהדק. טריגר
   שברירת המחדל שלו היא no-op הוא **נפילה שקטה**. נעול בבדיקה.
2. **אין `_trading_day_guard` בסקריפט.** ה-workflows כבר בודקים יום מסחר וטריות
   לכל שלב. שכפול כאן = מקור אמת שני שיסחף.
3. **204 בלבד = הצלחה.** 401 (PAT פג) · 403 (הרשאה) · 404 (repo/workflow) —
   כולם exit≠0. כשל חלקי (1 מתוך 2) הוא כשל, כי חצי צינור מת.

**ה-crons של GitHub נשארים.** חינם, לפעמים עובדים, וריצה כפולה היא no-op בזכות
הדדופ. רשת שנייה, לא כפילות.

### מה זה לא פותר

* **את האוסף** — תקלה נפרדת בבעלות המתכנת השני (01/09: `tase_putcall` קפואה על
  31/08 17:16, אפס שורות `pipeline_state`). טריגר מושלם לא מייצר נתונים.
* **תקלת Actions רוחבית** — dispatch חסין מפני המתזמן, לא מפני Actions שנופל.
* **אמינות Render** — לא אומתה. מחליפים מתזמן ללא התחייבות באחר; מכאן שה-crons
  של GitHub נשארים כגיבוי.
* **DST** — Render cron הוא UTC, בדיוק כמו GitHub.
* ~~שער הטריות החסר בתיקים 9/10~~ — ✅ תוקן 01/09, ראה 6ד.

### סימני אזהרה

| סימן | מסקנה |
|---|---|
| Render מראה ירי בזמן, אבל `gh run list` ריק | ה-API מחזיר שגיאה — PAT פג/הרשאה. בדוק את exit code של ה-cron |
| גם ריצות dispatch מאחרות | האבחנה שגויה; האיחור ביצירת ריצות בכלל. עבור להרצת הסקריפטים ישירות ב-Render |
| Render עצמו מאחר או לא יורה | החלפנו מתזמן במתזמן. Cloudflare Worker cron כשלישי |
| **שורות חדשות בתיקים 9/10 כש-`fetch_date != today`** | רגרסיה בשער הטריות (6ד). **עצור את הטריגר.** |
| פחות מ-3 ימי מסחר רצופים בזמן | לא מכריזים שנפתר |

---

## 6ד. ✅ שער טריות לתיקים 9 ו-10 — תוקן 01/09/2026

**הבאג.** `src/vwap_trader.py` נכתב ב-07–11/08, **אחרי** שאירוע תשעה באב
(23/07) הוסיף `is_chain_fresh` ל-`margin_recorder:212` ול-
`auto_open_benchmark_trades:160` — והוא לא קיבל את השער. תיקים 9 ו-10 היו
היחידים שיכלו לפתוח על שרשרת בת ימים.

**שני חלקים, לא אחד:**

| # | איפה | מה היה |
|---|---|---|
| 1 | `vwap_trader` — השרשרת | הבדיקה היחידה `if snap is None`. `fetch_date` נשמר ללוג ולסנאפשוט ו**מעולם לא נבדק** |
| 2 | `auto_open_vwap_trades:105` + `_latest_recommendation` | בורר לפי `max(recommended_at::date)` ו-`ORDER BY … DESC LIMIT 1`, **בלי השוואה ל-today**. ההערה מעל הקוד כבר כתבה "המלצה מהיום" |

**מדוע זה לא התפוצץ קודם.** כל עוד האוסף עובד, `fetch_date == today` ממילא,
והשער לא היה משנה דבר. הוא נדרש בדיוק ביום שבו האוסף נופל — כלומר במקרה
שהוא נועד לתפוס.

**החשיפה כפי שנמדדה** (dry-run, 01/09, כשהאוסף היה תקוע על 31/08 17:16):
הקוד ניגש לתמחר על שרשרת בת שלושה מושבים ונעצר **רק** כי אף רגל לא תומחרה.
המסנן היחיד שפעל היה `priced.complete` — **הסתברות, לא שער.** ביום שבו
שרשרת ישנה כן תתמחר במלואה, הייתה נפתחת פוזיציה על מחירים בני ימים.

### התיקון

* `is_chain_fresh(snap["fetch_date"], today)` **לפני** `fetch_traded_quotes`.
  המיקום מהותי: אחרי התמחור זו כבר הסתברות. בדיקה נועלת שאין פנייה לתמחור.
* `recommended_at::date = CURRENT_DATE` בבורר, ו-`= :today` ב-
  `_latest_recommendation` — **בשני המקומות בכוונה.** התיקון בפונקציה מגן על
  כל קורא, כולל אוטומציה עתידית שלא תעבור דרך הסקריפט.
* `_today_il()` = `datetime.now(TZ_IL).date()`, שעון ישראל כמו ב-
  `margin_recorder`. נקרא **פעם אחת** ל-`open_vwap_condor` ומועבר הלאה; שתי
  קריאות יכולות ליפול משני צדדיו של חצות ולסתור זו את זו. נעול בבדיקה.
* "אין המלצות מהיום" עלה ל-`WARN` ונוקב בשלב 2 — דילוג שנקרא כמו "יום שקט"
  מסתיר רושם שנכשל.

**אומת מול ה-DB החי** (01/09, שרשרת 31/08 17:16):
`שרשרת אינה טרייה — נמשכה 2026-08-31 17:16 (trade_date 28/08/2026),
היום 2026-09-01. לא נפתחת עסקה.` — ברמת ERROR, לא INFO.

**בדיקות: 1346 (13 חדשות), ונבדקו במוטציה** — ביטול השער ⇒ 6 נופלות ·
העברתו אחרי התמחור ⇒ הבדיקה הייעודית נופלת · החזרת הבורר ל-`max(...)` ⇒
נופלת · הסרת גבול התאריך מ-`_latest_recommendation` ⇒ נופלת.

> ⚠️ **שלושה שערים חופפים, וזה מכוון.** ב-01/09 כל אחד מהם עצר את הריצה
> באופן עצמאי. אל "תנקה" אחד מהם כי "השני ממילא תופס" — הם מכסים תקלות
> שונות: האוסף נפל · שלב 2 נכשל · קורא ישיר שאינו עובר דרך הסקריפט.

---


## 7. אופק הזמן — הבעיה הגדולה ביותר שנותרה פתוחה (02/08/2026)

### 7.1 הממצא

`hold_probability` מחושב על `expiry_history.move_pct`, שהוא תנועת **מושב אחד**.
אבל פוזיציה נפתחת מספר מושבים לפני הפקיעה. נמדד על **1,993 מושבי TA-35** (10 שנים):

| מרווח | מושב 1 | 3 מושבים | 5 מושבים | 7 מושבים |
|---|---|---|---|---|
| 1.75% | 95.3% | 74.0% | 60.9% | 54.6% |
| **2.25%** | **97.5%** | 84.1% | **72.2%** | 64.8% |
| 3.00% | 99.1% | 91.6% | 82.9% | 75.8% |

והמרווח שבאמת נדרש כדי לעמוד ב-`floor=0.97`:

```
מושב אחד 2.10%  ·  3 מושבים 4.05%  ·  5 מושבים 5.60%  ·  7 מושבים מעל 6%
```

**המנוע בוחר 1.75%–2.25%.**

**√t אינו תחליף:** σ בפועל גדולה ב-16%–29% מתחזית √t (מגמתיות). קנה-מידה
פרמטרי היה מקטין את הסיכון בכ-30%.

### 7.2 ⚠️ `floor=0.97` כויל על האופק הלא נכון

`margin_backtest._run_walkforward` משתמש ב-`actual_move = row["move_pct"]`
([margin_backtest.py:130](src/margin_backtest.py)) — כלומר `hold_rate` שהוא
מדווח הוא **שיעור החזקה למושב אחד**. זה ה-backtest שעליו כויל ה-floor
(23 שבירות מול 71 ב-0.96), וגם `wing_pct=0.75`.

**זה מיישב את הסתירה מסעיף 4.5:** *"96.19% hold, אבל מרווח קבוע 1.75% שובר
~25% מהשבועות"*. שני המספרים נכונים — לאופקים שונים.

### 7.3 העוגן שייך ל-T-1

`base_index` מגיע מ-`find_atm` על מחירי השרשרת, והשרשרת מייצגת את `trade_date`.
אומת על 37 snapshots:

```
ATM משתמע מול סגירת אותו יום   : |ממוצע| 1.008%  σ 1.237%
ATM משתמע מול סגירת היום הקודם : |ממוצע| 0.116%  σ 0.138%     ← פי 9 הידוק
```

> **מה שנשקל ונדחה:** להזריק מדד חי כעוגן. **אל תעשה את זה.** כל השרשרת היא
> T-1 — גם המדד המשתמע וגם מחירי האופציות (`trade_date=30/07` עבור
> `fetch_date=31/07`). מרכוז strikes על מדד של היום מול פרמיות של אתמול שובר
> את העקביות הפנימית ומחמיר. `baserate_call` הוא **NULL בכל הארכיון**.

**מה שכן נעשה:** האופק נספר מ-`trade_date` (`_anchor_date` ב-`margin_recorder`).
החשיפה מתחילה שם, ולכן היא מושב אחד ארוכה ממה שספירה מהיום מראה.

**השלכה מעשית:** האופק המינימלי האפשרי הוא **2 מושבים**, לא 1 — גם כניסה ביום
הפקיעה. לכן "לקצר החזקה למושב אחד" **אינו פתרון זמין** עם מבנה הנתונים הנוכחי.

### 7.4 מה נבנה

- `move_distribution.horizon_move_distribution(sessions, k)` — התפלגות לאופק k.
  `entry=close[i−k]`, `settle=open[i]`. מחזיר את אותו מבנה מילון ולכן משתלב
  ישירות עם `hold_probability_at_margin`.
- `src/index_series.py` — סדרת TA-35 היומית מ-Yahoo. כשל רשת ⇒ `[]`, לא חריגה.
- `margin_recorder._horizon_hold` — שומר בכל המלצה את `horizon.hold_at_margin`
  לצד `hold_blended`, עם `anchor_date` ו-`horizon_sessions`.

**הבחירה לא השתנתה.** `selected_margin` ו-`hold_blended` זהים בדיוק עם ובלי
הסדרה — `test_does_not_alter_selected_margin` מקבע זאת. הרחבת המרווח או הורדת
ה-floor היא **החלטה עסקית**, לא שינוי שקט.

בפועל היום (02/08, `trade_date=30/07`, מרווח 1.75%):

| פקיעה | אופק | hold מדווח | hold אמיתי |
|---|---|---|---|
| 04/08 | 3 מושבים | 97.6% | **63.6%** |
| 06/08 | 5 מושבים | 97.6% | **47.5%** |
| 07/08 | 6 מושבים | 97.6% | **42.2%** |

המספר המדווח **זהה לשלושתן** — הוא אינו רגיש לאופק כלל.

### 7.5 ❌ ה-backtest עם פרמיות אמיתיות — לא הצליח

נבנה ב-scratchpad (לא בקומיט). הרעיון: לכל פקיעה ואופק, לטעון את השרשרת
ההיסטורית מהארכיון ולחשב P&L אמיתי דרך `margin_pnl`.

**למה אי אפשר לסמוך עליו:**
- הארכיון מחזיק **38 ימי מסחר בלבד** (22/05–31/07/2026). n=18–29 לתא.
- התקופה חריגה — המדד ירד מ-4505 ל-4046, כ-10%.
- גם אחרי יישור האופק, ה-hate נמוך משמעותית מהמדידה על 10 שנים (76% מול 91%
  ל-2 מושבים ב-2.25%), **והשארית לא יושבה.**

**מה נדרש כדי להכריע:** מקור פרמיות היסטורי ארוך. אין. הארכיון גדל ביום מסחר
בכל יום — בעוד 2–3 חודשים תהיה תשובה אמינה.

### 7.7 ❌ גם ההצלבה על עסקאות אמיתיות לא עובדת — ואל תנסה שוב

אחרי תיקון `opened_at` נראה שאפשר לבדוק ישירות: האם עסקאות שנפתחו רחוק יותר
מהפקיעה נשברו יותר? זה **לא ניתן לביצוע** על הנתונים הקיימים.

Short Iron Condor, 26 עסקאות סגורות, לפי ימי החזקה:

| ימים | n | Win | ממוצע |
|---|---|---|---|
| 1 | 1 | 100% | +62 |
| 2 | 1 | 0% | ‎-1,759 |
| 3 | 2 | 0% | ‎-1,672 |
| 4 | 2 | 0% | ‎-1,603 |
| **7** | **20** | **65%** | **+246** |

נראה כאילו החזקה **ארוכה** עדיפה — וזה מטעה. **אין וריאציה אמיתית באופק:**
20 מתוך 26 נפתחו 7 ימים לפני הפקיעה, כי זה היום הראשון שהפקיעה מופיעה בשרשרת.
ארבע העסקאות הקצרות הן מ-16–19/06 — שבוע בודד שבו השוק ירד חזק. כלומר ההבדל
בטבלה הוא **בין שבועות**, לא בין אופקים.

כדי לבדוק את זה באמת צריך לפתוח את אותה פקיעה במספר אופקים במקביל. המערכת
אינה עושה זאת, ולכן הנתון הזה לא יצטבר מעצמו.

### 7.6 ההחלטה שנשארה

| אפשרות | מה נותן | מה עולה |
|---|---|---|
| להרחיב את המרווח | 97% אמיתי | ~5.6% במקום 1.75% — פרמיה נמוכה בהרבה |
| להוריד את ה-floor | פרמיה נשמרת | לקבל במפורש ש-hold ריאלי הוא 42%–78% |
| ~~לקצר החזקה~~ | — | **לא זמין** — ראה 7.3 |

**ההמלצה: לא להחליט עכשיו.** המספר האמיתי נשמר בכל המלצה, כך שהפער מצטבר על
נתונים אמיתיים. כשהארכיון יגדל — backtest אמין יכריע.

---

## 8. מחקר: מרווח מול כאוס — מתי כדאי מה (02/08/2026)

> ⚠️ **כל ה-P&L בסעיף הזה נמדד לפני תיקון הסילוק (06/08).** הכיוונים המבניים
> עומדים; **הגדלים לא אומתו מחדש.** ראה סעיף 11.4.

"כאוס" = **Long Straddle (5) + Long Strangle (6)** — מרוויחות מתנועה חדה.
"מרווח" = **Short Iron Condor (2)** — מרוויח מדשדוש.
המונח הוא של המשתמש; השתמשו בו.

השאלה שנחקרה: כשהמרווח נשבר, האם הכאוס מכסה — ואפשר לדעת מראש?

### 8.1 ✅ החזקת שניהם היא **לא** גידור — היא מכונת הפסד

26 פקיעות עם P&L אמיתי (16/06–31/07/2026):

| מצב | פקיעות | סה"כ משולב |
|---|---|---|
| מרווח הרוויח | 14 (54%) | **‎-48,152 ₪** |
| מרווח הפסיד, כאוס כיסה | 10 (38%) | **+43,548 ₪** |
| שניהם הפסידו | 2 (8%) | ‎-791 ₪ |

```
מרווח בלבד  -3,334 ₪  ·  כאוס בלבד  -2,062 ₪  ·  שניהם  -5,396 ₪
```

**מתאם מרווח↔כאוס: ‎-0.915** — נדנדה כמעט מושלמת. ודווקא לכן החזקת שניהם
מבטלת את עצמה ומשאירה את העלויות. דוגמה: 25/06, תנועה ‎-0.08% — המרווח +906,
הכאוס **‎-8,503**.

**האזור המת קיים אך זניח:** |תנועה| 2.47%–2.60%, 2 מתוך 26, ‎-791 ₪ בסך הכל.
זו לא הבעיה. הבעיה היא 14 השבועות שבהם הכאוס דימם.

**האסימטריה שכן לטובתנו:** הפסד המרווח **חסום** (max_loss), רווח הכאוס **לא** —
30/07 נתן ‎+13,041 ₪ בעסקה אחת. לכן אות חלש עשוי להספיק: לא צריך לצדוק ברוב
המקרים, צריך לצדוק כשזה גדול. **זה טרם נבדק** (דורש EV עם פרמיות).

### 8.2 ❌ `implied_move` — **לא נבדק, לא נפסל.** n=8 בלבד

`implied_move` נשמר בכל המלצה מאז 12/07 ומסומן "תיעוד בלבד". החפיפה עם תוצאות
סגורות היא **8 פקיעות**. זה חסר תוחלת סטטיסטית.

מה שנמדד (ואין להסתמך עליו): מתאם ‎-0.484, implied בשבועות כאוס 1.20% מול 2.18%
בשבועות מרווח — בכיוון ההפוך. דיוק סף 50% מול בסיס 62%.

> ⚠️ **מלכודת שנפלתי בה — אל תחזרו עליה.** בגרסה הראשונה השוויתי את ההמלצה
> **האחרונה** לפני הפקיעה (אופק יום אחד, implied≈0.63%) לתנועה של שבעה ימים
> (2.65%), וקיבלתי "הטיה" של ‎+1.89%. זה היה ארטיפקט של אופק.
> `expected_move_pct` הוא התנועה **עד הפקיעה** (`implied_daily = expected/√days`),
> ולכן חייבים להצליב מול ההמלצה **מיום הכניסה בפועל**.

**מה נדרש:** 30–40 פקיעות, כלומר 2–3 חודשי צבירה. קורה מעצמו.

### 8.3 ✅ התקבצות תנודתיות — קיימת ביומי, כמעט לא בפקיעות

| סדרה | n | מתאם lag-1 של \|תנועה\| |
|---|---|---|
| פקיעות שבועיות (עד 2026) | 708 | **+0.077** |
| פקיעות יומיות (2026) | 92 | +0.010 |
| **תשואות יומיות, 10 שנים** | 1,992 | **+0.193** |

ביומי האפקט חד:

```
אחרי |תנועה| > 2.0% → P(הבאה > 2.0%) = 15.8% מול בסיס 5.7%  (פי 2.76)
אחרי |תנועה| > 3.0% → P(הבאה > 3.0%) = 16.7% מול בסיס 1.8%  (פי 9.22)
```

**המסקנה:** ההתקבצות דועכת מהר. דגימה פקיעתית (שבוע בין נקודות) כמעט מפספסת
אותה. זו הסיבה ש-`recent_move` חלש.

### 8.4 ✅ `recent_move` נחות מאות תנודתיות יומית

1,967 נקודות · אופק 5 מושבים · סף כאוס 2.5% (מהמדידה ב-8.1) · שיעור בסיס 24%:

| אות | AUC | הפרדה עליון−תחתון |
|---|---|---|
| `recent_move` — **הנוכחי במנוע** | 0.600 | +16.0 נק' |
| ממוצע \|תשואה\| 5 מושבים | 0.653 | +28.7 |
| סטיית תקן 5 מושבים | 0.654 | +28.9 |
| **סטיית תקן 10 מושבים** | **0.657** | **+29.2** |

חמישון תחתון P(כאוס)=**8.6%** · עליון **37.8%**.
שלוש גרסאות האות היומי כמעט זהות — יציב, לא תלוי בבחירת הפרמטר.

> ⚠️ **AUC 0.657 הוא הטיה, לא מתג.** גם בחמישון העליון המרווח מנצח ב-62%
> מהמקרים. האות מזיז הסתברות — הוא אינו קובע תוצאה.

### 8.5 מה נבנה

`move_distribution.daily_volatility(sessions, window=10, before_date=None)` —
σ יומית + **מיקום בהתפלגות ההיסטורית** (`percentile`, `quintile`). המיקום הוא
העיקר: `σ=1.23%` לבדו חסר משמעות, "אחוזון 79" אומר משהו.

נשמר ב-`margin_recorder` תחת `horizon.daily_vol`, **לצד** `recent_move`.
**הבחירה לא השתנתה** — מתעדים, משווים על נתונים חיים, ורק אז מחליטים.

בבדיקה חיה (02/08) שני האותות **סותרים**: `recent_move`=0.612% (רגוע) מול
אחוזון 79.4 (סוער) — עקבי עם הרצף ‎-2.5%/-4.9%/-3.7% ב-29–31/07 ש-`recent_move`
מפספס. n=1 אינו ראיה; הערך הוא שהשוואה תיצבר אוטומטית.

### 8.6 מה שחוזר על עצמו — וצריך לקבל את זה

**שלוש פעמים בסשן אחד** התשובה נחסמה באותו מקום: אין פרמיות היסטוריות מספיקות.
ה-backtest (7.5), ההצלבה על תקופת החזקה (7.7), ו-EV של האות (8.4).

`tase_putcall_history` התחיל 22/05/2026 ומוסיף יום מסחר בכל יום. **אין קיצור
דרך** — כל ניסיון לעקוף את זה במדגם קטן החזיר תוצאה שלא ניתן לסמוך עליה.

---

## 9. סבב מחקר שני — 02/08/2026

> ⚠️ **כל ה-P&L בסעיף הזה נמדד לפני תיקון הסילוק (06/08).** 9.1 (הפער בין
> ה-hold המדווח לממומש) ו-9.2 (השוואת שקלים אינה תקפה) עומדים ומתחזקים.
> מספרים אחרים — ראה סעיף 11.4.

חקירה מקבילה בשש זוויות. הסבב הראשון איבד 18 מ-32 סוכנים (מאמת לכל ממצא →
תקיעות); הסבב השני עם **מאמת אחד לכל זווית** רץ 9/9 נקי. הלקח התפעולי: לפרוס
פחות ולתחום תקציב.

### 9.1 🔴 המספר החשוב ביותר — המערכת מדווחת 97.7%, בפועל 40%–70%

| מקור | ערך | n |
|---|---|---|
| `hold_blended` — טענת המנוע | **0.9773** (0.966–0.993) | 49 |
| `/margin` בדשבורד — "hold בפועל" | **100%** (לפני התיקון) | 10 |
| תיק 8 — תנועה ≤ מרווח מומלץ | **60%** | 10 |
| benchmark condor — תנועה ≤ 2.0% | **46%** | 26 |
| אחרי תיקון ה-validator | **70%** | 10 |

Wilson 95% על 4/10 → [16.8%, 68.7%]. ה-n קטן מכדי לקבע ערך מדויק, **וגדול לגמרי
מספיק כדי לדחות את 97.7%**. זו האישור הראשון על עסקאות ממומשות לתחזית סעיף 7.

### 9.2 🔴 השוואת P&L בשקלים בין 6 האסטרטגיות **אינה תקפה**

הון לעסקה נע פי **14.7**: Straddle 4,526 · Strangle 2,180 · Condor 1,352 ·
BCS 687 · CallFly 313 · PutFly 307.

תשואה על הון פרוס:

| | P&L | **ROC** |
|---|---|---|
| Long Straddle | +2,158 | **+1.83%** |
| Long Strangle | ‎-4,220 | ‎-7.45% |
| Short Iron Condor | ‎-3,334 | ‎-9.49% |
| Long Put Butterfly | ‎-1,796 | **‎-22.51%** |
| Long Call Butterfly | ‎-1,960 | **‎-24.07%** |
| Bull Call Spread | ‎-6,778 | ‎-37.96% |

**שני הפרפרים יורדים מ-#2/#3 ל-#4/#5.** "כמעט מאוזנים" הוא ארטיפקט של סיכון
~307 לעסקה. "רק Long Straddle רווחית" **שורד** — אך אחרי drawdown של 28,530
(Calmar-דמוי 0.076).

### 9.3 ✅ שער כלכלי — `required_hold` (נבנה, כבוי)

`required_hold = 1 − credit/width`. על 49 המלצות: credit/width ממוצע **0.21**
⇒ המבנה דורש ~79% רק כדי לא להפסיד. **33/49 (67%) תוחלת שלילית ב-hold כן.**
אחת עם `net_premium = -2.0` — קונדור short **בדביט**.

`margin_selector.required_hold` / `binary_ev`. `net_premium ≤ 0` נדחה תמיד;
`ev_hold` **כבוי כברירת מחדל** — הפעלה ב-0.70 חוסמת 69% מההמלצות.

### 9.4 ⛔ אות התנודתיות שנבנה באותו יום — **מת במשטר החי**

| תקופה | n | AUC std10 |
|---|---|---|
| כל העשור (מה שדווח ב-9.3 של סעיף 8) | 1,967 | 0.657 |
| 2016–2021 | 1,045 | 0.712 |
| 2022–2026 | 922 | 0.530 |
| **מ-08/2024 (החי)** | **412** | **0.507** |

ה-0.657 הוא ניגוד בין 2017 (6.2% כאוס) ל-2020 (37.7%) ו**אינו משוחזר באף שנה**.
bootstrap: CI95 [0.430, 0.570], P(AUC>0.60)=0.005.
EWMA(λ=0.94) 0.511 · פערי פתיחה 0.447 — כולם נכשלים.

> **הקו סגור.** לא מחוסר בדיקה אלא כי אין מה למצוא. **המנוף אינו ניבוי משטר
> אלא גודל פוזיציה ובחירת מבנה.** סעיף 8.4 מתוקן בהתאם.

### 9.5 שני תיקונים לעבודה קודמת

- **73/166 "פריצות" של `max_loss` הן ארטיפקט.** `pnl` נטו מעמלה, `max_loss` ברוטו.
  השארית אחרי החזרת עמלות **בדיוק 0.000**. אל תבנו התראה על זה.
- **ל-Yahoo חסרים כל ימי ראשון.** 1,993 ברים = 199/שנה מול ~245 של TASE; `Sun=0`.
  ⚠️ אבל **אל תיישמו תיקון +4pp** — ה-72.2% אינו תואם לא ל-k=4 ולא ל-k=5, כי הוא
  מודד `close[i-k]→open[i]` ולא close-to-close. לקרוא איך הוא נגזר לפני שנוגעים.

### 9.6 nulls — אל תריצו שוב

- **עמלות אינן הבעיה.** 520 מול הפסד 3,334 = 15.6%, ו-2.76% מהקרדיט.
- **`compute_balance` תקין** — דלתא 0.0 בכל 7 התיקים.
- **יום בשבוע לא משפיע.** הפער הגולמי הוא ארטיפקט: Mon/Sun/Fri קיימים רק מ-2024/2026.
  **אל תוסיפו day-of-week** — תקבעו בקוד את משטר 2024–26.
- **אין ברך בעקומת דעיכת ה-hold.** מונוטונית ‎-0.120 → ‎-0.032.
- **W/M — כיוון null.** M-W ‎-0.03pp, CI [‎-0.166, +0.106]. המגניטודה אמיתית, הכיוון לא.
- **skew שלילי אינו מצדיק כנף put רחבה.** ב-2.5% יחס תוחלת ההפסד 1.03 — מאוזן.
  שולט רק מ-~3.5%, מעבר לטווח שבשימוש.
- **הטיית snapping** ‎-0.046pp — שני סדרי גודל מתחת ל-credit/width. זניח.
- **"למרכז מחדש ב-+0.35%"** — drift של שוק שורי, לא תכונה. הימור כיווני בתחפושת.
- **"פילטר קרדיט מינימלי"** — משפר את pf3 ומחמיר את pf8. לא משכפל.

### 9.7 חסום

1. **פרמיות היסטוריות לפני 22/05/2026** — קשיח. חמישה ניסיונות מתו על זה.
2. **gating מבוסס-סיגנל** — סגור במשטר החי (9.4).
3. **סייזינג בסיכון קבוע** — מבטיח (Strangle ‎+13,321 בנרמול!) אבל **2 מ-26 עסקאות
   נושאות 84.7%**. לעולם לא לצטט בלי סולם הקיטום.
4. **drawdown תוך-שבועי** — כל ה-DD כאן ממומש בלבד; ה-mark-to-market גדול יותר.

---

## 10. הלקח המרכזי

שתי התקלות הגדולות שנמצאו כאן לא היו שגיאות בקוד. שתיהן היו **היעדר** —
ובשתיהן הכל היה ירוק.

> **תשעה באב: המערכת לא הפסיקה לאסוף נתונים. היא הפסיקה לדעת שהיא לא מקבלת אותם.**
>
> **ה-benchmark: הקוד עבד מצוין. פשוט לא היה מי שיפעיל אותו.**

האוסף עשה בדיוק את הדבר הנכון ועצר. שלוש שכבות אצלנו — ה-cron, הלואדר, והרושמים —
המשיכו לעבוד כאילו כלום, כי אף אחת מהן לא ידעה לשאול "האם בכלל הייתי אמור לקבל
נתונים היום?".

ותיקי ה-benchmark חיכו שש שבועות ללחיצת כפתור בממשק שאינו פרוס — בזמן שגשר 2,
שכל תפקידו להשוות את המנוע למציאות, ישב בלי מציאות להשוות אליה.

וכשמשהו כן נשבר (6 עסקאות פתוחות שישה שבועות), התגובה הייתה להשתיק את ההתראה
במקום לחקור אותה — על בסיס אבחון שהתברר כשגוי.

**ארבעה כללים שנגזרים מזה:**
1. הצלחה שקטה מסוכנת יותר מכישלון רועש. אם מסלול לא יכול להאדים — הוא לא מנוטר.
2. רשומה ברשימת "ידוע/מוכר" חייבת סיבה, תאריך, ודיווח בכל ריצה. אחרת היא לא
   תיעוד — היא הסתרה.
3. **פונקציה שאין לה קורא מתוזמן היא פונקציה שלא רצה.** לפני שמניחים שפיצ'ר
   פועל — לבדוק מי קורא לו, ומאיפה. `grep` על שם הפונקציה הוא הבדיקה הזולה
   ביותר בפרויקט הזה, והיא זו שחשפה את 3.6.
4. **אבחנה שנרשמה ולא תורגמה לתיקון היא אבחנה שלא נעשתה** (נוסף 06/08). באג
   הסילוק היה מתועד ב-CLAUDE.md וב-HANDOFF 2, כולל המשפט המפורש "וכך גם
   `close_index` של עסקאות שנסגרו אוטומטית". הוא נשאר חי שבוע כי סווג
   "בעיה של המערכת השנייה". ראה סעיף 11.

---

## 11. באג הסילוק ומחיר המילוי — 06–11/08/2026

הסעיף החשוב במסמך. הוא מבטל מסקנות מסעיפים 8–9 ומגדיר מה חוסם את המחקר.

### 11.1 🔴 כל העסקאות נסגרו במחיר הלא נכון

`paper_db.get_settlement_index` ו-`get_expiries_ready_to_close` קראו מ-
`condor_settled_detail.actual_index_close`. הערך הזה הוא **סגירת המושב הקודם**.

אופציות ה-TA-35 השבועיות מסתלקות ב**מחיר הפתיחה של יום הפקיעה**. אומת בשלוש
מדידות בלתי תלויות על 28 פקיעות (10/06 → 31/07), כולן שגיאה 0.00:

| השוואה | תוצאה |
|---|---|
| `aic` == `expiry_history.base_price` | 28/28 |
| `aic` == נעילת המדד של המושב הקודם (Yahoo) | 28/28 |
| `expiry_history.expiry_price` == פתיחת יום הפקיעה (Yahoo) | 28/28 |

**גודל השגיאה = פער הלילה:** 24 נקודות מדד חציונית, **0.59%**. על רצועת מרווח
של 1.75% זה כשליש מרוחב הרצועה — די כדי להפוך זכייה להפסד ליד הגבול.

**התיקון:** `expiry_history.expiry_price` כמקור ראשי, עם גיבוי לפתיחת המדד מ-
`index_series` (הטבלה הרשמית מפגרת כמה ימים אחרי הפקיעה, והסגירה צריכה לרוץ
באותו יום). אין מחיר סילוק ⇒ הפקיעה לא נסגרת, עם שורת לוג. `aic` אינו משמש יותר
בשום מסלול, ובדיקות רגרסיה נועלות את זה.

**התיקון ההיסטורי:** `scripts/reprice_closed_trades.py` (dry-run כברירת מחדל).
187 עסקאות, **43 הפיכות סימן**. גיבוי: `paper_trades_backup_reprice_20260806_153311`.
הסקריפט חורג ביודעין מהכלל ש-`close_trade` הוא מסלול ה-UPDATE היחיד — הוא מתקן
שלוש עמודות בעסקאות **שכבר סגורות**, מה ש-`close_trade` אינו יכול לעשות.

### 11.2 🔴 `lastrate` אינו מחיר עסקה, ו-80% מהשורות לא נסחרו כלל

זו הבעיה שחוסמת הכל, וגדולה מבאג הסילוק. **קרא גם 11.2ב — יש פתרון חלקי.**

| מדד | ערך |
|---|---|
| שורות ב-`tase_putcall_history` ללא עסקה באותו יום (CALL) | **80.8%** |
| אותו דבר, PUT | **78.6%** |
| רגליים של עסקאות אמיתיות ללא עסקה באותו יום | **73.7%** (115/156) |
| `lowrate`/`highrate` ברגל כזו | **NULL** — אין טווח נסחר בכלל |

המילוי שלנו הוא `lastrate`, ו-**זה בוודאות אינו מחיר עסקה** (07/08/2026):

```
lastrate בתוך הטווח [lowrate, highrate] של אותו יום —
  CALL  1,468/2,660  (55.2%)
  PUT   1,753/2,932  (59.8%)
```

מחיר עסקה אחרון אינו יכול ליפול מחוץ לטווח העסקאות של אותו יום. הוא נופל שם
ב-45%/40% מהמקרים — **ואלה דווקא הסטרייקים שכן נסחרו.** לכן `lastrate` הוא ציטוט
או שער תיאורטי, לא מילוי.

### 11.2ב ✅ VWAP — מחיר עסקה אמיתי שכבר יש לנו

```
VWAP = OverallTurnOverValue_Shekel / OverallTurnOverUnits
```

| | בתוך טווח [low, high] של אותו יום |
|---|---|
| **VWAP** | **2,697/2,697 CALL · 2,995/2,995 PUT — 100.0%** |
| `lastrate` | 54.4% · 58.5% |

100.0% אינו קירוב — זו הוכחה שהחישוב נכון ושהערך הוא מחיר עסקה משוקלל.
הפער מ-`lastrate`: **51.5% חציוני ממחיר האופציה.**

**זה נגזר משדות שכבר שמורים ב-`tase_putcall_history`.** אין צורך בשינוי בפיד,
במתכנת השני, או במוצר בתשלום. **זו הדרך לתמחר כל עסקה חדשה במחיר אמיתי.**

מגבלה: VWAP הוא ממוצע יומי, לא מילוי בנקודת זמן. הוא מחיר שנסחר, לא מחיר מובטח.

### 11.2ג ⛔ bid/ask אינם קיימים במקור — נבדק ישירות

נמשך ה-endpoint שהפיד משתמש בו
(`POST https://api.tase.co.il/api/derivatives/putvscall`, גוף
`{"objId":"01","lang":0,"dType":2,"date":"DD/MM/YYYY","pageNum":1}`):

- **36 שדות. אפס** קשורים ל-bid/ask/ספר פקודות.
- השוואה מול העמודות שלנו: **אנחנו שומרים את כולם.** ה-whitelist אינו זורק כלום.
- `curr_Hour = "סוף יום"` ב-14,264/14,264 השורות — פיד סוף-יום. ספר פקודות לא יהיה בו.

**bid/ask יגיעו רק מ-TASE DataWise (בתשלום) או מ-API של ברוקר.** שאלה למשתמש.

### 11.2ד 🔴 המלכוד — נזילות ויתרון גרים באופקים שונים

הנזילות מרוכזת בפקיעה הקרובה. הרגליים של העסקאות שלנו, לפי אופק:

| ימים לפקיעה | רגליים | במחיר עסקה אמיתי |
|---|---|---|
| 1 | 8 | **100%** |
| 2 | 16 | **75%** |
| 3 | 20 | 0% |
| 4 | 16 | 12% |
| **7** (המנוף ששרד) | **108** | **18%** |

**ברמת העסקה: 5 מתוך 43 קונדורים ניתנים לתמחור מלא, וכולם באופק 1–2 ימים.
אפס מתוך 27 באופק 7.**

והאופק שבו יש נזילות מלאה הוא זה שבו `required_hold` הוא 98.7% (k=1) — שלילי מבנית.
החיתוך היחיד שיש בו גם נזילות וגם פער לא-שלילי הוא **k=2**: required 83.6% מול
hold היסטורי 84.5%, ו-75% כיסוי. **מעולם לא נבדק בכיול נכון** — ארבע העסקאות
שיש לנו שם נפתחו עם מרווח שכויל ל-5 מושבים.

חמש העסקאות בנות-התמחור המלא, לשם הסדר (כולן 1–2 ימים, כולן שליליות):
`lastrate` ‎-4,906 (ROC ‎-62.7%) · VWAP ‎-4,291 (‎-54.9%) · אדוורסרי ‎-5,369 (‎-68.6%).

### 11.3 המנוף היחיד ששרד — לא הופרך, וגם לא אושר

חקירה של שישה מנופים (אופק החזקה · יציאה מוקדמת · הטיה כיוונית · מרומז מול ממומש ·
גידור זנב · רוחב עם תוחלת חיובית) השאירה אחד: **אופק ההחזקה**. כל ההפסד בקונדור
בא מפוזיציות שנפתחו קרוב לפקיעה — קרוב לפקיעה הפרמיה קורסת אבל רוחב הסיכון נשאר.

```
אופק 7 ימים (5 מושבים)   n=27   ROC בסילוק הישן  +21.0%
                                 ROC בסילוק הנכון +14.0%
אופק 2–4 ימים            n=13   שלילי בכל חתך
```

> ⚠️ **תוקן 07/08/2026.** הגרסה הראשונה של הסעיף הזה קבעה "היתרון מתאפס בקנס של 5%,
> והקנס הנצפה הוא 43% — אין יתרון נמדד". **זה היה מחמיר מדי, ומסיבה מודלית.**
>
> אותה מדידה מידלה את שגיאת המילוי כ**עלות חד-כיוונית** — קונים ביוקר ומוכרים בזול
> בכל רגל בנפרד. מול מחירי עסקה אמיתיים (11.2ב) מתברר שהשגיאה היא בעיקר **הזזת רמה**:
> `lastrate` גבוה מהמחיר שנסחר ב-10 מתוך 18 הרגליים, חציון ‎-17.8%. במבנה של ארבע
> רגליים שבו מוכרים שתיים וקונים שתיים, הזזת רמה **מתקזזת חלקית**; קנס חד-כיווני לא.
>
> **הלקח המתודי:** לפני שמכריזים על יתרון כמת, לבדוק אם מודל הרעש מתאים למבנה.
> רעש שמתקזז בין רגליים אינו מתנהג כמו עמלה.

**התמחור מחדש עם VWAP (07/08/2026):**

| תרחיש | ROC באופק 7 ימים |
|---|---|
| `lastrate` בכל מקום (המדידה הקודמת) | +14.0% |
| VWAP איפה שיש, `lastrate` בשאר | +10.3% |
| + שאר הרגליים בחציון הפער הנצפה (‎-17.8%) | +1.9% |
| **bootstrap, חציון** | **+9.6%** |
| bootstrap, רווח סמך 95% | ‎-13.8% … +33.9% |
| bootstrap, P(ROC>0) | 78.7% |

**המסקנה: היתרון לא הופרך וגם לא אושר.** רווח הסמך חוצה את האפס, ו-78.7% אינו סף
שמצדיק החלטה. שתי הסתייגויות שחייבות ללוות כל ציטוט של המספרים האלה:

1. ה-bootstrap מודד **רק** אי-ודאות מילוי — לא את אי-הוודאות של 27 עסקאות על 8 שבועות.
   האי-ודאות האמיתית **גדולה** מהטווח למעלה.
2. נדגמו 18 פערים נצפים בלבד, בטווח ‎-57% עד ‎+115%.

**ומעל הכל — האופק הזה אינו בר-אימות:** אפס מתוך 27 העסקאות ניתנות לתמחור מלא
(11.2ג). כל השורה הזו נשענת על אקסטרפולציה מ-18 רגליים.

### 11.4 מה זה מבטל

- **"כל האסטרטגיות מפסידות"** (סעיף 0 הישן) — לא נכון. Long Straddle ‎+2,698.
- **track record של תיק 8** — היה ‎+150, בפועל ‎-2,535.
- **כל מסקנה בסעיפים 8–9 שנשענת על P&L בשקלים** — המספרים השתנו. הכיוונים
  המבניים (8.1 החזקת שניהם אינה גידור, 9.2 השוואת שקלים אינה תקפה) עומדים.
- **קורלציה קונדור↔כאוס ‎-0.915 ואזור המוות** — נמדדו על ה-P&L הישן. הכיוון
  (אנטי-קורלציה חזקה, אזור מוות צר) נשמר, הגדלים לא אומתו מחדש.
- **"אין יתרון נמדד" (הגרסה הראשונה של 11.3)** — בוטל 07/08. ראה את התיקון שם:
  מודל הרעש היה שגוי. היתרון אינו מאושר, אבל גם אינו מופרך.

### 11.5 מה חוסם, ומה הצעד הבא

**מה שנסגר 07/08:** `bid`/`ask` **אינם קיימים במקור** (11.2ג — נבדק ישירות מול
ה-API, 36 שדות, אפס). אל תחזור לשאלה הזו. הם יגיעו רק מ-DataWise או מברוקר.

**מה שנפתח 07/08:** VWAP (11.2ב) הוא מחיר עסקה אמיתי, נגזר משדות שכבר יש לנו,
מאומת 100.0%. הוא אינו פותר את ה-80% חסרי המסחר — אבל הוא הופך את השאר למדיד.

**הצעד הבא, לפי סדר:**

1. ✅ **לתמחר עסקאות חדשות ב-VWAP** — **בוצע 07/08/2026, תיק 9. ראה 11.6.**
2. **להגביל פתיחה לסטרייקים עם מחזור בתיקים הקיימים.** תיק 9 עושה את זה אצלו;
   השאלה אם להחיל על תיק 8 היא **שינוי אופי — החלטת משתמש**, ולא בוצעה.
3. **לבדוק k=2 בכיול נכון** (11.2ד) — החיתוך היחיד שיש בו גם נזילות וגם פער חיובי.
4. **להעביר את באג הסילוק** (`_fetch_settlement_price`) — עדיין לא הועבר.
5. **גישה ל-API של ברוקר** — השאלה היחידה שתפתח את ה-80% מהסטרייקים שאין להם
   מחיר. פתוחה למשתמש.

**ההכרעה שמאחורי 2:** לסחור באופק שבו אפשר למדוד, או להמשיך באופק שאולי יש בו
יתרון אבל אי אפשר לאמת אותו. מערכת שלא ניתן למדוד לא תשתפר לעולם.

**אל תריץ סריקות פרמטרים נוספות על 8 שבועות.** ה-n חוסם, לא איכות הניתוח.

### 11.6 ✅ תיק 9 — התיק התאום במחירי עסקה (נבנה 07/08/2026)

**מה זה.** אותן המלצות מרווח בדיוק כמו תיק 8, אבל ממולאות ב-VWAP במקום
ב-`lastrate`. ההפרש בין שני התיקים בעוד שלושה חודשים הוא המדידה: **כמה
מהתוצאה של תיק 8 הייתה אמיתית וכמה מחיר מומצא.**

| | |
|---|---|
| תיק | 9 · "המלצות המערכת — תמחור VWAP" · הון 100,000 · עמלה 2.5 (זהים לתיק 8) |
| `strategy_id` | **103** |
| kill-switch | `VWAP_TRADING_ENABLED` (דלוק) |
| קוד | `src/vwap_pricing.py` (טהור) · `src/vwap_trader.py` · `scripts/auto_open_vwap_trades.py` |
| Action | `auto_open_vwap_trades` — ב'–ו' 09:00 UTC |

**הכלל היחיד: אין מחיר עסקה לכל 4 הרגליים ⇒ אין עסקה.** אין להרחיב את
הקריטריון כדי "לפתוח יותר" — הדילוג הוא הנתון. ריצה ראשונה: **1 מ-4**
(11/08 עברה, 12–14/08 דולגו). התיק יתכנס מעצמו לאופק הקצר, כי שם הנזילות.

> ⚠️ **תיק 9 ייראה גרוע מתיק 8 בטווח הקצר, וזה לא כישלון שלו.** הוא נכנס
> לפחות עסקאות ובעיקר לאופק 1–2 מושבים, שכבר ידוע כשלילי מבנית (11.2ד).
> הערך שלו הוא ההפרש, לא הרמה.

**בידוד — שלוש שכבות, לכל אחת בדיקה שנועלת אותה:**
1. `strategy_id=103`. כל אגרגציה חוצת-תיקים (`decision_validator`,
   `web/src/lib/data.ts`) חסומה ב-`strategy_id BETWEEN 1 AND 6` ⇒ 103 שקוף לה.
2. kill-switch נפרד. הדלקת `RECO_TRADING_ENABLED` **אינה** מדליקה אותו.
3. אפס קריאות לפונקציה שמשנה התנהגות בתיקים 2–8. גם `get_latest_option_chain`
   לא נגעה — המודול עושה שאילתה משלו, דווקא כדי לא לגעת במסלול החי.

אומת אחרי הכתיבה: תיקים 2–7 עם 34 עסקאות כל אחד, תיק 8 עם 14 — ללא שינוי.

**שני מוקשים שנתפסו בהרצה הראשונה** (לא הגיעו ל-DB, ולכל אחד נעילת רגרסיה):
- `margin_pct` חוזר מ-`margin_recommendations` כ-**`Decimal`**, ו-`paper_db._dumps`
  מסדר את ה-snapshot ב-`json.dumps` **בלי `default`** — בכוונה, כדי לא לשנות
  התנהגות לתיקים הקיימים. הפתרון הוא ב-payload, לא בשכבת הכתיבה המשותפת.
- **`insert_trade` קושר 19 שדות, כולל NULL-ים** (`closed_at`, `pnl`, `pnl_pct`,
  `exit_commission`). שדה חסר נכשל ב-`"A value is required for bind parameter"` —
  הודעה שנראית כמו כשל DB ולא כמו שדה שנשכח. `vwap_trader.TRADE_FIELDS` נועל
  את הסט. **מי שכותב מסלול פתיחה חדש — התחל משם.**

**ההשוואה הראשונה — קיימת מ-11/08/2026.** אותן פקיעות בדיוק, שני מקורות מחיר:

| פקיעה | תיק | קרדיט | max_loss | hold נדרש |
|---|---|---|---|---|
| 12/08 | 8 (`lastrate`) | 10.00 | 1,990.00 | **99.5%** |
| 12/08 | **9 (VWAP)** | **145.82** | 1,854.18 | **92.7%** |
| 13/08 | 8 (`lastrate`) | 177.00 | 1,323.00 | 88.2% |
| 13/08 | **9 (VWAP)** | **291.95** | 1,208.05 | **80.5%** |

הפער ב-hold הנדרש: **6.8–7.7 נקודות** על אותה עסקה בדיוק. ובפקיעת 12/08 —
`lastrate` נתן קרדיט של **10 ₪** מול 145.82 ₪ במחיר עסקה, פי 14.6.

זו עדות ראשונה (n=2) לכך שחלק מ"ההפסדים" ההיסטוריים של תיק 8 הם עסקאות
שנרשמו בקרדיט מומצא-נמוך. **אל תסיק מזה עדיין** — צריך עשרות זוגות.
אבל שים לב שגם ב-92.7% זו עסקה גרועה: ה-hold האמיתי באופק 5 מושבים הוא ~61%.

---

### 11.7 🔴 הבאג שהשבית את תיק 9 בשקט — ולקח שחוזר

**מה קרה.** `fetch_traded_quotes` קרא מ-`tase_putcall_history` עם
`as_of = datetime.now().date()`. הארכיון נכתב **פעם ביום בסוף המסחר, ~17:15
שעון ישראל**, בעוד ש-`auto_open_vwap_trades` רץ ב-09:00 UTC = 12:00 שעון
ישראל — חמש שעות מוקדם יותר.

אין שורה בארכיון לתאריך של היום ⇒ אפס ציטוטים ⇒ כל פקיעה מדולגת.
**התיק לא היה פותח עסקה אף פעם באוטומט.** העסקה היחידה שהייתה בו נפתחה רק
כי הורצה ידנית ב-18:27, אחרי שהארכיון נכתב.

נמדד 11/08/2026 בשעה 16:06:

```
tase_putcall           fetch 2026-08-11 16:01  trade_date 10/08   ← קיים
tase_putcall_history   fetch 2026-08-10 17:16  trade_date 07/08   ← אחרון
מהארכיון: 0 עסקאות  ·  מהטבלה החיה: 2
```

**התיקון.** ברירת המחדל היא `tase_putcall` (החיה) והצילום האחרון בה.
`as_of` מפורש נשאר נתמך ל-backtest מול הארכיון.

> **הלקח, והוא זהה ללקח 1 בסעיף 10 בלבוש אחר.** סיבת הדילוג הייתה
> "4/4 רגליים ללא מחיר עסקה ב-2026-08-11" — שנקרא כמו **מסקנה על נזילות**,
> בעוד שהאמת הייתה "אין שרשרת לתאריך הזה בכלל". הודעת דילוג שאינה נוקבת
> ב**מקור** שעליו נבדקה היא הודעה שמסתירה תקלה מאחורי ממצא.
>
> מעכשיו הסיבה נוקבת בצילום, ו-`market_snapshot_json` שומר
> `chain_fetch_date`/`chain_fetch_time`/`chain_trade_date`.
> **כלל לכל מסלול חדש: שדה "מקור הנתון" אינו קישוט — בלעדיו אי אפשר להבחין
> בין "בדקתי ואין" לבין "לא בדקתי".**

### 11.8 ✅ הדלקת `RECO_TRADING_ENABLED` (11/08/2026)

תיק 8 לא פתח עסקה מ-30/07 עד 11/08 — **22 פקיעות**. המתג כובה ב-27/07 אחרי
אירוע תשעה באב ולא הוחזר אחרי שהשערים נבנו. הפער אינו ניתן לשחזור.

**שלושת השערים אומתו בקוד לפני ההדלקה, לא לפי מה שרשום כאן:**

1. `_trading_day_guard` (`auto_record_margins.py:103`) — חוסם את כל הריצה
   ביום שאינו יום מסחר.
2. `is_chain_fresh(fetch_date, today)` (`margin_recorder.py:212`) — נבדק
   **לכל פקיעה בנפרד**, ברירת מחדל 0 = "נמשכה היום" ⇒ `status='stale_chain'`.
3. **החוליה שסוגרת:** `_open_reco_trades` פותח רק לפקיעות בסטטוס
   `('recorded', 'skipped_exists')`. `'stale_chain'` אינו ברשימה — ולכן
   שרשרת ישנה אינה מגיעה לפתיחה **כלל**, לא רק מדווחת.

מצב הכשל המקורי — פתיחה על שרשרת ישנה — חסום במקור.

⚠️ **מה שלא נסגר:** שאלת ה-hold. `hold_probability` עדיין נמדד על תנועת
מושב אחד בעוד שהפוזיציה חשופה 5 (סעיף 7), והמנוע בוחר 1.75% במקום ש-2.5%
פחות גרוע (סעיף 12). ההדלקה נעשתה ביודעין למרות זאת: תיק 8 הוא **תיק דמו
למחקר**, וההשוואה שלו לתיק 9 היא כל מטרתו של תיק 9. תיק שאינו סוחר אינו
מייצר את הנתון הזה, וכל יום שעובר הוא פקיעות שלא ניתן להשוות בדיעבד.

---

## 12. למה המערכת מפסידה — התשובה (11/08/2026)

השאלה שנשאלה: *"איך זה הגיוני שאנחנו לא מרוויחים? כשעשיתי הכל בדמו עם 2%–2.5%
לכל כיוון, כמעט תמיד הרווחתי."*

### 12.1 76% זכיות זה הפסד — האריתמטיקה

ברצועה של 2.5%, לפי הפרמיות של היום:

```
מתוך 4 שבועות:   3.0 זכיות × 237 ₪   =   +722 ₪
                  1.0 הפסדים × 1,296 ₪ = -1,235 ₪
                  ──────────────────────────────
                                            -513 ₪
```

**הפסד אחד מוחק 5.5 זכיות.** התחושה "כמעט תמיד ניצחתי" מדויקת — 76% מהשבועות.
מה שלא נשמר בזיכרון באותה עוצמה הוא שבוע אחד מארבעה מחק את כל השאר.

### 12.2 כל רוחב רצועה שלילי — אבל רחב פחות גרוע

hold נדרש מול hold בפועל (10 שנים, 1,989 חלונות של 5 מושבים):

| רצועה | hold נדרש | hold בפועל | פער |
|---|---|---|---|
| **1.75%** (המנוע) | 72.0% | 60.9% | **‎-11.1** |
| 2.00% | 78.0% | 67.2% | ‎-10.8 |
| **2.50%** | 84.5% | 76.2% | **‎-8.4** |
| 3.00% | 90.7% | 82.9% | ‎-7.8 |

**אין רוחב שמציל.** הרחבה מדממת לאט יותר, לא עוצרת. והמנוע בוחר את הגרוע
מכולם — 1.75% — כי `hold` נמדד אצלו על **מושב אחד**, שם 1.75% כמעט אף פעם
לא נפרץ. הפוזיציה חשופה חמישה. זה באג האופק של סעיף 7, במספרים.

### 12.3 🔴 הממצא המרכזי — השוק השתנה

| תקופה | hold ב-2.5% | נדרש |
|---|---|---|
| 2016-08 → 2018-08 | **92.2%** | 84.5% → **רווחי** |
| 2018-08 → 2020-09 | 77.3% | |
| 2020-09 → 2022-08 | 74.3% | |
| 2022-08 → 2024-09 | 71.5% | |
| **2024-09 → 2026-08** | **65.6%** | 84.5% → **קטלני** |

**האסטרטגיה עבדה.** ב-2016–2018 רצועת 2.5% החזיקה 92.2% מהשבועות. מאז זה
נשחק **בכל תקופה ברציפות** — ירידה של 26 נקודות.

> ⚠️ **הסתייגות שחייבת ללוות כל ציטוט:** עמודת "נדרש" מחושבת מפרמיות של
> **היום**. ב-2016–2018 התנודתיות הייתה נמוכה ולכן גם הפרמיות, כלומר ה-hold
> הנדרש אז היה כנראה גבוה מ-84.5% והיתרון האמיתי קטן מ-7.7 נקודות.
> **מה שמוצק:** שיעור ה-hold עצמו, שנמדד ישירות מהמדד. לא נבדק מה היו
> הפרמיות אז — אין ארכיון שרשראות לפני 22/05/2026 (סעיף 9.7).

### 12.4 מה זה מסביר

- **למה כל האסטרטגיות שליליות** — לא באג, שוק שנע יותר ממה שהאופציות מתמחרות.
- **למה דווקא קוני התנודתיות סבלו פחות** — Long Straddle הוא הכי פחות שלילי
  (‎-1.0% ROC) מול Short Iron Condor (‎-7.2%). אותו סיפור משני כיוונים.
- **למה הזיכרון של המשתמש נכון** — הוא סחר במשטר אחר.

### 12.5 מה לבדוק הלאה

**מרומז מול ממומש, בכיוון ההפוך.** אם השוק מתמחר תנודתיות של 2% ובפועל היא
2.8%, אז מכירת מרווחים היא הצד הלא נכון של העסקה במשטר הזה — והצד הנכון
הוא בדיוק ההפך. זה יסביר את 12.2 ואת 12.4 בבת אחת, וזה ניתן למדידה.
חסום כרגע על n (סעיף 8.2), אבל תיק 9 מייצר בדיוק את הנתון הדרוש.

---

## 13. נזילות וביצוע — מה אפשר לדעת, ומה לא (11/08/2026)

השאלה שנשאלה: *"לא תמיד יהיה קונה כשארצה למכור. איזה נתונים אפשר לקבל?"*
הרקע — ניסיון מכירה אמיתי בחשבון פרטי ב-16:30 (נעילה ~17:00) נשאר בלי קונה.

### 13.1 שלושה דברים שונים, ולכל אחד תשובה אחרת

| | היסטורית | קדימה |
|---|---|---|
| **ספר פקודות** (יש קונה עכשיו?) | ✗ | ✗ **גם לא בעתיד** |
| **מחזור יומי לכל סטרייק** | ✅ 44 ימי מסחר | ✅ |
| **עקומה תוך-יומית** (לפי שעה) | ✗ | ✅ מ-11/08 (13.4) |

⛔ **ספר פקודות אינו קיים ולא יהיה מהמקור הזה** — נבדק ישירות (11.2ג):
36 שדות, אפס רלוונטיים, 6 endpoints סבירים החזירו 404, ו-`curr_Hour="סוף יום"`
בכל השורות. **אל תחזור לשאלה.** יגיע רק מ-DataWise או מ-API של ברוקר.

### 13.2 משטח הנזילות — הגורם הוא האופק, לא המרחק מהכסף

P(הסטרייק נסחר), 44 ימי ארכיון, עוגן מסדרת המדד לפי `trade_date`:

| מרחק מהכסף | 1 יום | 2 ימים | 3–4 | 5+ |
|---|---|---|---|---|
| ±0.5% | **100%** | 98% | 37% | 13% |
| 1–1.5% | **100%** | 99% | 36% | 15% |
| 2–2.5% | **100%** | 98% | 31% | 20% |
| 3–4% | 99% | 91% | 25% | 11% |
| מעל 4% | 44% | 40% | 12% | 3% |

**יום–יומיים לפני הפקיעה כל סטרייק בטווח 4% נסחר.** מעבר לזה — הימור.
המרחק מהכסף כמעט לא משנה עד 4%, ומעליו קורס.

### 13.3 🔴 "נסחר" אינו "אפשר לצאת" — עומק

ברגליים 1–2 ימים לפקיעה ועד 4% מהכסף (n=3,110 שנסחרו):

```
חציון 128 יחידות/יום · אחוזון 75 = 691 · אחוזון 25 = 28 · אחוזון 10 = 6
20% מהרגליים שנסחרו הציגו פחות מ-20 יחידות
34% הציגו פחות מ-50
```

רגל עם 2 יחידות ביום **נסחרה** — ופקודה אחת שם היא 50% מהמחזור היומי שלה.
זו ככל הנראה חוויית ה-16:30: לא היעדר נזילות אלא נזילות שאינה סופגת פקודה.
(ב-3+ ימים לפקיעה: חציון 6 יחידות, 69% מתחת ל-20.)

### 13.4 ✅ ארכוב תוך-יומי — מה שנבנה

`tase_putcall` מתעדכנת כל ~15 דק'; `tase_putcall_history` שומרת **צילום אחד
ביום ב-~17:15**. ההתפתחות התוך-יומית נזרקה — ואיתה התשובה על השעה.

  · טבלה `tase_putcall_intraday` · `src/intraday_archive.py`
  · Action `archive-intraday-chain` — כל 30 דק', ב'–ו', 06:00–15:30 UTC
  · אידמפוטנטי על **הצילום של המקור** (`fetch_date`+`fetch_time`), לא על זמן
    הריצה. Action שנדחה או רץ פעמיים אינו מייצר כפילות — אומת בפועל.
  · `tase_putcall` — SELECT בלבד, עם בדיקה שסורקת כל SQL שהמודול מריץ.

**מתי יהיה מה לנתח:** ~שבועיים. עקומת מחזור לפי שעה לכל סטרייק.
**לא לנתח לפני** — עקומה על יומיים היא רעש.

### 13.5 ✅ תיק 10 — סינון נזילות + אילוץ הון

| | |
|---|---|
| תיק | 10 · "המלצות המערכת — סינון נזילות" |
| **הון** | **20,000 ₪** (לא 100,000 כמו השאר — בבקשת המשתמש) |
| עמלה | 2.5 ₪/רגל, בלי מינימום — **אומת מול מבנה הברוקר בפועל** |
| `strategy_id` | **104** · kill-switch `LIQUIDITY_TRADING_ENABLED` |
| סינון | `min_units = 50` — חוזה אחד ≤2% מהמחזור היומי של הרגל |

**ההבדל מתיק 9 הוא מספר אחד.** שניהם רצים על אותו מסלול קוד
(`vwap_trader.PortfolioSpec`); קוד כפול היה נסחף והופך את ההשוואה למדידה
של הבדלי מימוש במקום של הסינון.

**שער ההון** (`vwap_trader.available_capital`), חל על **כל** התיקים:

    הון פנוי = הון התחלתי + Σ P&L(סגורות) − Σ |max_loss|(פתוחות)

`max_loss` הוא הביטחונות — בקונדור מוגדר-סיכון הברוקר מקפיא את ההפסד המרבי
עד הפקיעה. כשל DB ⇒ None ⇒ **לא נפתחת פוזיציה** (fail-safe מכוון).

> ⚠️ **בחוזה אחד לפקיעה השער כמעט לא ייגע.** קונדור תופס ~1,205 ₪ בממוצע,
> והשיא ההיסטורי הוא 5 פוזיציות = ~6,500 ₪ מתוך 20,000 (33%). המגבלה
> תיאורטית כרגע; היא תבלום רק בסייזינג גדול יותר. **אל תדווח עליה כאילו
> היא פעילה.**

**הנתון שכן יבלוט: עמלות.** 20 ₪ סבב מלא על קרדיט של 145.82 ₪ = **13.7%**.
ובעסקאות עם קרדיט נמוך זה מכריע — בפקיעת 13/08 הקרדיט לפי `lastrate` היה
10 ₪, כלומר עמלות של 200%.

### 13.6 שלושת התיקים — מה כל אחד מודד

| תיק | תמחור | סינון | הון | מה ההשוואה נותנת |
|---|---|---|---|---|
| 8 | `lastrate` | — | 100k | בסיס. מחיר שאינו סחיר |
| 9 | VWAP | נסחר בכלל | 100k | 8↔9 = כמה מתיק 8 היה מחיר מומצא |
| 10 | VWAP | נסחר בעומק (50) | **20k** | 9↔10 = מה עולה לדרוש עומק |

**אל תשנה פרמטרים באמצע.** כל שינוי מאפס את ההשוואה, וזה כל מה שהתיקים
האלה קיימים בשבילו.
