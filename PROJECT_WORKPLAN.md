# TA-35 Expiry Intelligence — קובץ תוכנית עבודה מרכזי

> **מטרה:** קובץ זה הוא "מפת הדרכים" של הפרויקט — איפה עצרנו ומה הבא.
> בסוף כל פגישה — מעדכנים את סעיף 7.
>
> **חלוקת עבודה בין המסמכים:**
> [CLAUDE.md](CLAUDE.md) = ארכיטקטורה וסטטוס מודולים ·
> [AGENTS.md](AGENTS.md) = כללי בטיחות ·
> [HANDOFF.md](HANDOFF.md) = הקשר תפעולי שאינו בקוד ·
> **הקובץ הזה = מפת דרכים**. אל תשכפל ביניהם.
>
> **עודכן:** 02/08/2026

---

## 1. זהות הפרויקט

**שם:** TA-35 Expiry Intelligence Dashboard
**מטרת-על:** מערכת ניתוח הסתברותי של פקיעות מדד TA-35, המלבישה 6 אסטרטגיות אופציות
על נתונים היסטוריים, ומייצרת תמונת מצב הסתברותית לפקיעה הקרובה.

---

## 2. מה המערכת עושה (ובמה היא לא עוסקת)

### עושה:
- ניתוח **993** פקיעות היסטוריות, 2010 → היום
- הלבשת 6 אסטרטגיות אופציות על כל פקיעה היסטורית
- בחירת רוחב Short Iron Condor אופטימלי לפקיעה קרובה (מנגנון המרווח)
- הצלבה עם אירועים וחדשות — הסבר לפקיעות קיצוניות
- מסחר-דמו אוטומטי לפי המלצות (תיק 8), **כרגע כבוי**
- שכבת ולידציה: המנוע מול המציאות (hit-rate, regret)

### לא עושה (בכוונה):
- לא מבצע עסקאות אמיתיות — **הכל דמו וירטואלי**
- לא מבטיח רווח
- **אינו כלי מסחר.** כלי מחקר בלבד; אין בפלט המלצת השקעה או ייעוץ פיננסי

---

## 3. הנתונים

### 3.1 נתונים היסטוריים — `expiry_history`

**993 שורות**, 25/02/2010 → 31/07/2026. שני מקורות, ניתנים להפרדה:

| מקור | כמות | סימן זיהוי |
|---|---|---|
| CSV היסטורי (`market_data.csv`) | 965 | `close_price IS NOT NULL` |
| משוחזר מסילוק + Yahoo | 28 | `close_price IS NULL` |

> 🔴 **תיקון עובדתי (31/07/2026) — היה כתוב כאן הפוך.**
>
> `בסיס` (`base_price`) **אינו** "מחיר המדד בפתיחת יום הפקיעה". הוא **סגירת
> המושב הקודם** — אומת `base_price[i] == close_price[i-1]` על 12 מתוך 12.
> `פקיעה` (`expiry_price`) הוא מחיר הסילוק, שהוא הפתיחה של יום הפקיעה
> (אומת 6/6 מול Yahoo, עד האגורה).
>
> **המשמעות:** `move_pct` הוא תנועת **מושב אחד** — מסגירה לסילוק. כל שימוש בו
> כאילו הוא תנועה של שבוע או של החזקה רב-יומית שגוי, ושגוי לכיוון האופטימי.

**חישוב:** `move_pct = (פקיעה − בסיס) / בסיס · 100`

### 3.2 נתוני אופציות לפקיעה קרובה

- טבלת `tase_putcall` ב-Supabase — snapshot יחיד, מתעדכן כל 15 דקות בימי מסחר.
- ההיסטוריה ב-`tase_putcall_history` (snapshot אחד ליום).
- **מי כותב:** `tase-pipeline` — **מערכת של מתכנת שני** על אותו DB. ראה HANDOFF.md.
- מכפיל: 50 ש"ח לנקודה.

---

## 4. שש האסטרטגיות

| # | שם | כיוון | מנצח כאשר |
|---|----|----|-----------|
| 1 | Bull Call Spread | שורי | `move_pct > 0` |
| 2 | Short Iron Condor | ניטרלי | `abs_move < X%` |
| 3 | Long Call Butterfly | ניטרלי-ממוקד | `abs_move` קטן מאוד |
| 4 | Long Put Butterfly | ניטרלי-ממוקד | `abs_move` קטן מאוד |
| 5 | Long Straddle | תנודתיות | `abs_move > X%` |
| 6 | Long Strangle | תנודתיות | `abs_move > X%` (OTM) |

תיקי ה-benchmark 2–7 מריצים אותן בפרמטרים **מוקפאים**. אל תשנה — הם קו הבסיס.

---

## 5. ארכיטקטורה — בפועל

| רכיב | מה יש |
|---|---|
| DB | **Supabase PostgreSQL** (דרך pooler) — לא SQLite |
| דשבורד פרודקשן | **Next.js** תחת `web/`, פרוס ב-Render מ-`main` |
| Streamlit (`app.py`, `pages/`) | קיים, **לא פרוס** — כלי פיתוח מקומי |
| אוטומציה | **4** GitHub Actions, **שני–שישי** (`cron '... 1-5'`) |
| איסוף שרשרת | `tase-pipeline` — Docker → Render Background Worker, **repo נפרד** |
| מדד וסילוק | Yahoo `TA35.TA` (ה-API של TASE חסום ב-WAF) |

---

## 6. תוכנית עבודה — לפי שלבים

### ✅ שלב 1 — תשתית + ניתוח היסטורי — **הושלם**
- [x] הקמת סביבה, `requirements.txt`, חיבור DB
- [x] טעינת `market_data.csv` (עברית, `utf-8-sig`, ניקוי), חישוב `move_pct`
- [x] ניתוח סטטיסטי: התפלגות, עליות/ירידות, פילוח W/M, שנה, יום בשבוע

### ✅ שלב 2 — הלבשת אסטרטגיות — **הושלם**
- [x] מחלקה לכל אסטרטגיה + `is_success(move_pct, params)` + grid search
- [x] Backtest על כל הפקיעות, Win Rate, קיצונים
- [x] דשבורד השוואת אסטרטגיות

### ✅ שלב 3 — שכבת אירועים — **הושלם**
- [x] טבלת `events` + 22 אירועים
- [x] ניתוח השפעה, ציון סיכון, השוואה עם/בלי אירוע

### ✅ שלב 4 — פקיעה קרובה + אופציות — **הושלם**
- [x] פרסור שרשרת, זיהוי ATM
- [x] `context_analyzer` — מצב שוק דומה + המלצה

### 🔄 שלב 5 — אוטומציה ופרודקשן — **חלקי**
- [x] איסוף נתוני בורסה יומי — **נעשה ע"י `tase-pipeline` (מערכת נפרדת)**
- [x] מעבר ל-PostgreSQL (Supabase)
- [x] Deploy לשרת (Render, Next.js מ-`main`)
- [ ] **אבטחה — login בסיסי.** לא נעשה. הדשבורד פתוח.

### ✅ שלב 6 — אמינות ומוניטורינג — **הושלם 31/07–01/08/2026**

נבנה בתגובה לאירוע תשעה באב. פירוט מלא ב-HANDOFF.md סעיף 3.

- [x] `src/trading_calendar.py` — לוח מסחר (`is_trading_day`, `trading_days_between`,
      `skip_reason`, `is_chain_fresh`). **הבורסה עברה לשבוע שני–שישי ב-2026**;
      ה-cron תוקן מ-`0-4` ל-`1-5`.
- [x] שער יום-מסחר בשני הרושמים; `FORCE_RUN=true` לעקיפה ידנית.
- [x] שער טריות — שרשרת שאינה של היום ⇒ `stale_chain`, אפס כתיבה, **exit≠0**.
- [x] `_report_orphans` — עסקה פתוחה מעל 2 ימי מסחר אחרי הפקיעה ⇒ **exit≠0**.
- [x] שערי שפיות ב-`history_updater` (`find_shared_bases`, `is_plausible_move`).
- [x] ניקוי 27 שורות מזוהמות ושחזורן נכון (`scripts/backfill_expiry_history.py`).
- [x] סגירת 6 עסקאות יתומות מ-19/06.

### ✅ שלב 7 — אוטומציית benchmark + מדידת אופק — **הושלם 02/08/2026**

- [x] **תיקי 2–7 מעולם לא שוגרו אוטומטית.** `open_trades_for_expiry` נקראה רק
      מ-Streamlit שאינו פרוס. `scripts/auto_open_benchmark_trades.py` + workflow.
- [x] **132 עסקאות שוחזרו** מהארכיון, ‎`_NO_SETTLEMENT` מחריג פקיעות שלא התרחשו.
- [x] **`opened_at` תוקן** ב-132 השורות (אומת דרך `atm_level`, 132/132).
- [x] **`hold` לפי אופק** — `horizon_move_distribution` + `src/index_series.py`.
- [x] **העוגן שייך ל-T-1** — האופק נספר מ-`trade_date`, לא מהיום.
- [x] **אות תנודתיות יומית** — `daily_volatility`, נשמר לצד `recent_move`.

### ⏳ שלב 8 — הבא לביצוע

**דורש מקור חיצוני (לא ניתן לפתור מהקוד):**
1. **אימות ערבי החג בספטמבר** (11/09, 25/09) מול לוח TASE הרשמי.
   חסומים כברירת מחדל; 21/09 (יום כיפור) ודאי.
2. **בירור פקיעת 23/07 מול TASE** — גולגלה או בוטלה? חוסם את סגירת עסקה 36.
3. **העברת ממצא הסילוק למתכנת השני** — `actual_index_close` הוא סגירת המושב
   הקודם (27/27). משפיע על `actual_pnl_ils` של **כל** פקיעה.

**חסום עד שהארכיון יגדל (2–3 חודשים):**
4. **שאלת ה-`floor`.** המנוע מדווח 97.6%, המציאות 42%–78% תלוי באופק.
   ⚠️ `floor=0.97` ו-`wing_pct=0.75` כוילו על **האופק הלא נכון**
   (`margin_backtest:130` משתמש ב-`move_pct` = מושב אחד).
   שלוש דרכי קיצור נוסו ונחסמו — ראה HANDOFF 7.5, 7.7, 8.6. **אין קיצור דרך.**
5. **EV של אות התנודתיות.** AUC 0.657 מזיז P(כאוס) מ-24% ל-38% — הטיה, לא מתג.
   האם זה מיתרגם לכסף תלוי בפרמיות שאין.
6. **`implied_move` כמנבא** — n=8 בלבד. לא נבדק, לא נפסל. צריך 30–40.
7. **החזרת `RECO_TRADING_ENABLED`** — לא לפני שיש EV. המנוע ממליץ Short Iron
   Condor, שהפסידה ‎-3,334 ₪ על 26 עסקאות למרות 54% Win Rate.

**ניתן לביצוע עכשיו:**
8. **`abs_move_pct` ≠ `|move_pct|`** (סעיף 8.1) — מוקש. 962/986 שורות.
9. **`days_to_expiry` ב-`market_snapshot_json`** שגוי ב-132 השורות המשוחזרות.
   לא תוקן בכוונה (ה-snapshot משקף את רגע הכניסה) — לחשב מ-`expiry_date − opened_at`.
10. **login בסיסי** (שארית שלב 5).

---

## 7. סטטוס פגישות עבודה

| תאריך | מה עשינו | הבא |
|-------|----------|-----|
| 18/05/2026 | הגדרת מטרות, ניתוח קבצים, ארכיטקטורה, תוכנית עבודה | Milestone 1.1 |
| 14/07/2026 | מנגנון המרווח (שלבים 1–6) + גשר 3 חי ב-main; guard על הוולידציה; שדרוג Actions | ניטור המלצות ועסקאות |
| **01/08/2026** | **אבחון אירוע תשעה באב ותיקונו.** גילוי שהבורסה עברה לשבוע ב'–ו' וה-cron היה שגוי מתחילת השנה; ניקוי 27 שורות מזוהמות מ-`expiry_history` ושחזורן; לוח מסחר, שער טריות וגילוי יתומות; סגירת 6 עסקאות יתומות; עדכון שלושת מסמכי ההוראות | אימות ערבי החג בספטמבר; בירור 23/07 מול TASE |
| **02/08/2026** | **אוטומציית benchmark + מחקר מרווח-מול-כאוס.** גילוי שתיקים 2–7 מעולם לא שוגרו אוטומטית (Streamlit לא פרוס) — 132 עסקאות שוחזרו ונסגרו, track record אמיתי לראשונה; `hold` נמדד על מושב אחד לפוזיציה שחשופה 5–6, והעוגן שייך ל-T-1; `recent_move` (AUC 0.600) נחות מסטיית תקן יומית (0.657) | שאלת ה-`floor` — חסומה עד שהארכיון יגדל |

---

## 8. הערות טכניות חשובות

### 8.1 ⚠️ עמודת "אחוז" — **ההנחה שהייתה כאן שגויה**

היה כתוב: *"עמודת אחוז היא ערך מוחלט תמיד — כיוון נגזר מ-(פקיעה − בסיס)/בסיס"*.

**נבדק 31/07/2026:** `abs_move_pct` (עמודת "אחוז") **אינו** `|move_pct|`.
הוא נבדל ממנו ב-**962 מתוך 986** שורות — ממוצע 1.28% מול 0.49%. הוא גם אינו
`|daily_pct|` (2/965 התאמות) ואינו `|open_pct|` (5/965). **כמות רביעית, לא מתועדת.**

**כרגע לא מזיק:** `_abs_move_series` מזין רק את סיווג ה-`regime` ואת שדה הדיווח
`abs_mean`. בחירת המרווח נשענת על `move_pct` — העמודה הנכונה.
**אבל זה מוקש.** אל תשתמש ב-`abs_move_pct` כאילו הוא הערך המוחלט של התנועה.

### 8.2 שאר ההערות
2. שורה אחרונה בקובץ ההיסטורי כתוב "פקיעה ראשונה" — לדלג עליה
3. שורות עם סוג `-` — פקיעות לא מזוהות, לסמן כ-unknown
4. מכפיל אופציות: **50 ש"ח לנקודה**
5. `putvscall.csv` מועיל יותר מ-`derivativesall` (מכיל מחירים)
6. encoding קבצי בורסה: `utf-8-sig`
7. **`condor_settled_detail` הוא VIEW** שמסנן `is_valid=true`. שורות שנפלו ממנו
   הותירו 6 עסקאות פתוחות שישה שבועות. `is_valid=false` אינו נדיר (~60 מול ~212)

---

## 9. מנגנון המרווח + מסחר-לפי-המלצות — ✅ הושלם (14/07/2026)

### מנגנון המרווח (שלבים 1–6)
`payoff.py` (P&L ל-condor) · `margin_calculator.py` (עקומת המרווח, גריד 1.0–3.0%) ·
`move_distribution.py` (התפלגות + `hold_probability` + EV + conditioning) ·
`margin_selector.py` (בחירה) · `margin_validator.py` + `margin_backtest.py` ·
`margin_recorder.py` (רישום יומי, idempotent).

**פרמטרים מכוילים:** `floor=0.97` · `wing_pct=0.75` · `w=0.6`.
הרציונל והמספרים שהכריעו — HANDOFF.md סעיף 4.
⚠️ שלושתם כוילו על הקורפוס לפני התיקונים של 31/07, וכיול מחדש נמצא בשלב 7.

### שכבת "המנוע מול המציאות" (גשרים 1–2)
`decision_recorder.py` רושם החלטות; `decision_validator.py` מחשב hit-rate / regret.
**guard:** רק strategy_id 1–6 נספרות; תיק ההמלצות (102) מוחרג — בפייתון וב-TS.

### גשר 3 — מסחר-לפי-המלצות (דמו)
`recommendation_trader.py`, תיק 8. הגנות: kill-switch, דדופ, `min_days_to_expiry=1`,
strategy_id 102.

> ⛔ **`RECO_TRADING_ENABLED` כבוי מ-31/07/2026.** ההגנות הקיימות לא מנעו פתיחת
> עסקה על שרשרת ישנה — הפרצה שהתממשה בתשעה באב. נסגרה בשער הטריות; ההדלקה
> מחדש בשלב 7.

### בדיקות
- **1,185** בדיקות pytest עוברות (אומת 02/08/2026)
- 75 בדיקות vitest (Web) — לא אומת מחדש

---

## 10. מצב נוכחי בקצרה

| | |
|---|---|
| `expiry_history` | 993 שורות עד 31/07/2026 |
| המנוע | `regime=calm` · מרווח 1.75% · מדגם מותנה 315 |
| `paper_trades` | **194** — 166 סגורות · 28 פתוחות · `opened_at` נכון בכולן |
| Track record | 30 עסקאות לכל אסטרטגיה · benchmark **‎-15,929 ₪** |
| | רק Long Straddle ברווח (+2,158). Short Iron Condor: 54% Win אך ‎-3,334 ₪ |
| עסקאות יתומות | 36 בלבד (23/07, מוכרת) |
| בכל המלצה נשמר | `hold` מדווח **ו**אמיתי-לפי-אופק · `anchor_date` · `daily_vol` |
| `RECO_TRADING_ENABLED` | **כבוי** |
| `HISTORY_UPDATER_ENABLED` | **כבוי** (עד תיקון באג הסילוק ב-`tase-pipeline`) |

---

> ⚠️ **המערכת היא כלי מחקר בלבד** — לא המלצת מסחר, לא ייעוץ פיננסי, ולא ביצוע
> עסקאות. כל המספרים מתייחסים לתיקי דמו וירטואליים.
