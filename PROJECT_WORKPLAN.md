# TA-35 Expiry Intelligence — קובץ תוכנית עבודה מרכזי

> **מטרה:** קובץ זה הוא "מפת הדרכים" של הפרויקט.
> בכל פגישת עבודה — פותחים אותו קודם, רואים איפה עצרנו, ומתקדמים.
> בסוף כל פגישה — מעדכנים את הסטטוס.

---

## 1. זהות הפרויקט

**שם:** TA-35 Expiry Intelligence Dashboard
**מטרת-על:** מערכת ניתוח הסתברותי של פקיעות מדד TA-35, המלבישה 6 אסטרטגיות אופציות על נתונים היסטוריים, ומייצרת תמונת מצב הסתברותית לפקיעה הקרובה.

---

## 2. מה המערכת עושה (ובמה היא לא עוסקת)

### עושה:
- ניתוח 973+ פקיעות היסטוריות מ-2010 עד היום
- הלבשת 6 אסטרטגיות אופציות על כל פקיעה היסטורית
- חישוב הסתברות הצלחה לכל אסטרטגיה (כמה % מהפקיעות היו "בתוך הטווח")
- הצלבה עם אירועים וחדשות (קורונה, מלחמות, ריבית) — הסבר למה פקיעות קיצוניות היו קיצוניות
- לקראת פקיעה קרובה: ניתוח שרשרת אופציות + המלצה הסתברותית

### לא עושה (בכוונה):
- לא מבצע עסקאות אמיתיות
- לא מחשב PnL מדויק (אין מחירי אופציות היסטוריים עדיין)
- לא מבטיח רווח

---

## 3. הנתונים

### 3.1 נתונים היסטוריים — פקיעות
- **קובץ:** `market_data.csv`
- **תוכן:** 965 פקיעות תקפות מ-2010 עד מאי 2026
- **עמודות מפתח:**
  - `תאריך` — תאריך הפקיעה
  - `סוג` — W (שבועי) / M (חודשי)
  - `בסיס` — מחיר המדד בפתיחת יום הפקיעה
  - `פקיעה` — מחיר המדד ברגע הפקיעה
  - `אחוז` — ערך מוחלט של תנועת הפקיעה (תמיד חיובי!)
- **חישוב כיוון:** נגזר מהשוואה `פקיעה > בסיס` → עלייה / ירידה
- **חישוב `move_pct` עם סימן:** `(פקיעה - בסיס) / בסיס * 100`

### 3.2 נתוני אופציות לפקיעה קרובה
- **קובץ 1:** `derivativesall.csv` — שרשרת מלאה, עמודה אחת לכל אופציה
- **קובץ 2:** `putvscall.csv` — Call ו-Put זה לצד זה לפי סטרייק (מועיל יותר)
- **מכפיל:** 50 ש"ח לנקודה
- **תאריך עדכון:** מורד ידנית מאתר הבורסה (tase.co.il)
- **עתיד:** cron job שמוריד אוטומטית פעם ביום

---

## 4. שש האסטרטגיות

| # | שם | כיוון | מנצח כאשר |
|---|----|----|-----------|
| 1 | Bull Call Spread | שורי | המדד עולה (move_pct > 0) |
| 2 | Short Iron Condor | ניטרלי-חווט | abs_move < X% (נשאר בטווח) |
| 3 | Long Call Butterfly | ניטרלי-ממוקד | abs_move קטן מאוד (ליד ATM) |
| 4 | Long Put Butterfly | ניטרלי-ממוקד | abs_move קטן מאוד (ליד ATM) |
| 5 | Long Straddle | תנודתיות | abs_move > X% (תנועה חזקה לכל כיוון) |
| 6 | Long Strangle | תנודתיות | abs_move > X% (תנועה חזקה, OTM) |

**פרמטרים לחקירה (grid search):**
- Iron Condor: טווח 1%, 1.5%, 2%, 2.5%, 3%
- Butterfly: רוחב כנפיים 20, 40, 60, 80 נקודות
- Bull Call Spread: רוחב 10, 20, 30, 50 נקודות
- Strangle: מרחק 0.5%, 1%, 1.5%, 2% מהמדד

---

## 5. ארכיטקטורה טכנית

### סביבה מקומית (עכשיו)
- **שפה:** Python 3
- **דשבורד:** Streamlit (רץ בדפדפן מקומי)
- **בסיס נתונים:** SQLite (קובץ אחד, ללא התקנה)
- **ספריות:** pandas, numpy, plotly, streamlit, sqlalchemy

### מבנה תיקיות
```
ta35-dashboard/
├── app.py                  # נקודת כניסה Streamlit
├── requirements.txt
├── database/
│   └── ta35.db            # SQLite
├── data/
│   ├── uploads/           # קבצי CSV שמועלים
│   └── current/           # שרשרת אופציות עדכנית
├── src/
│   ├── data_loader.py     # טעינה וניקוי CSV
│   ├── strategies.py      # לוגיקת 6 האסטרטגיות
│   ├── backtester.py      # הרצת backtest הסתברותי
│   ├── events.py          # שכבת אירועים/חדשות
│   └── charts.py          # גרפים (plotly)
├── pages/
│   ├── 1_historical.py    # ניתוח היסטורי
│   ├── 2_strategies.py    # השוואת אסטרטגיות
│   ├── 3_upcoming.py      # פקיעה קרובה
│   └── 4_events.py        # אירועים והקשר
└── PROJECT_WORKPLAN.md    # הקובץ הזה
```

### סביבת פרודקשן (עתיד)
- **שרת:** VPS (Hetzner / DigitalOcean) או Railway/Render
- **בסיס נתונים:** PostgreSQL (מעבר מ-SQLite בשינוי מינימלי)
- **אוטומציה:** cron job להורדת נתוני בורסה יומית

---

## 6. תוכנית עבודה מפורטת — לפי שלבים

---

### שלב 1 — תשתית + ניתוח היסטורי
**סטטוס: ⏳ הבא לביצוע**

#### Milestone 1.1 — הקמת סביבה
- [ ] יצירת תיקיית פרויקט `ta35-dashboard/`
- [ ] `requirements.txt` עם כל התלויות
- [ ] `app.py` בסיסי שעולה ב-Streamlit
- [ ] חיבור SQLite

#### Milestone 1.2 — טעינת נתונים היסטוריים
- [ ] דף העלאת CSV בדשבורד
- [ ] פרסור `market_data.csv` (עברית, encoding, ניקוי)
- [ ] חישוב `move_pct` עם סימן מ-בסיס/פקיעה
- [ ] שמירה לטבלת `expiry_history` ב-SQLite
- [ ] הצגת טבלה + סטטיסטיקה בסיסית

#### Milestone 1.3 — ניתוח סטטיסטי בסיסי
- [ ] התפלגות תנועות (גרף)
- [ ] % עליות vs ירידות
- [ ] % פקיעות לפי גודל תנועה (0-1%, 1-2%, 2%+)
- [ ] פירוט לפי שבועי/חודשי
- [ ] פירוט לפי שנה / חודש / יום בשבוע

---

### שלב 2 — הלבשת אסטרטגיות
**סטטוס: 🔒 ממתין לשלב 1**

#### Milestone 2.1 — לוגיקת אסטרטגיות
- [ ] מחלקה לכל אסטרטגיה עם פרמטרים ניתנים להגדרה
- [ ] פונקציית `is_success(move_pct, params)` לכל אסטרטגיה
- [ ] grid search על כל הפרמטרים

#### Milestone 2.2 — Backtest הסתברותי
- [ ] הרצת כל וריאנט על כל 965 הפקיעות
- [ ] חישוב Win Rate, ממוצע תנועה, קיצונים
- [ ] טבלת תוצאות ב-SQLite

#### Milestone 2.3 — דשבורד השוואת אסטרטגיות
- [ ] טבלה: אסטרטגיה | Win Rate | פרמטר מיטבי
- [ ] גרף השוואה חזותי
- [ ] פילטור לפי סוג פקיעה / שנה / תנאים

---

### שלב 3 — שכבת אירועים וחדשות
**סטטוס: 🔒 ממתין לשלב 2**

#### Milestone 3.1 — בניית בסיס אירועים
- [ ] טבלת `events` ב-SQLite
- [ ] הכנסת אירועים ידנית (קורונה 2020, מלחמת 2023, ריבית וכו')
- [ ] קישור כל פקיעה לאירועים סביבה (7 ימים לפני)

#### Milestone 3.2 — ניתוח השפעת אירועים
- [ ] השוואת תנועות עם/ללא אירוע
- [ ] הסבר אוטומטי לפקיעות קיצוניות
- [ ] News Risk Score לפקיעה קרובה

---

### שלב 4 — פקיעה קרובה + אופציות
**סטטוס: 🔒 ממתין לשלב 3**

#### Milestone 4.1 — טעינת שרשרת אופציות
- [ ] פרסור `putvscall.csv` מהבורסה
- [ ] זיהוי ATM, סטרייקים רלוונטיים
- [ ] הצגת שרשרת בדשבורד

#### Milestone 4.2 — המלצה לפקיעה קרובה
- [ ] זיהוי "מצב שוק דומה" בהיסטוריה
- [ ] הצגת הסתברות לכל אסטרטגיה בהתחשב בהקשר
- [ ] דוח מסכם לפקיעה הקרובה

---

### שלב 5 — אוטומציה ופרודקשן
**סטטוס: 🔒 ממתין לשלב 4**

- [ ] cron job להורדת נתוני בורסה
- [ ] מעבר מ-SQLite ל-PostgreSQL
- [ ] Deploy לשרת
- [ ] אבטחה (login בסיסי)

---

## 7. סטטוס פגישות עבודה

| תאריך | מה עשינו | הבא |
|-------|----------|-----|
| 18/05/2026 | הגדרת מטרות, ניתוח קבצים, גיבוש ארכיטקטורה, כתיבת תוכנית עבודה | Milestone 1.1 — הקמת סביבה |
| 14/07/2026 | מנגנון המרווח הושלם (שלבים 1–6) + גשר 3 (מסחר-לפי-המלצות דמו) חי ב-main; guard על שכבת הוולידציה; שדרוג גרסאות Actions (checkout@v5 / setup-python@v6) | ניטור ההמלצות והעסקאות; המשך פרודקשן |

---

## 8. הערות טכניות חשובות

1. **עמודת "אחוז" בקובץ ההיסטורי היא ערך מוחלט תמיד** — כיוון נגזר מ-`(פקיעה - בסיס) / בסיס`
2. **שורה 967** בקובץ ההיסטורי כתוב "פקיעה ראשונה" — לדלג עליה
3. **שורות עם סוג `-`** — פקיעות לא מזוהות, לטפל בנפרד
4. **מכפיל אופציות:** 50 ש"ח לנקודה
5. **putvscall.csv** הוא הקובץ המועיל יותר מ-derivativesall (מכיל מחירים)
6. **encoding קבצי בורסה:** `utf-8-sig` עם תווי עברית

---

## 9. קבצים בפרויקט

| קובץ | תיאור | מיקום |
|------|-------|-------|
| `market_data.csv` | פקיעות היסטוריות 2010-2026 | `data/uploads/` |
| `derivativesall.csv` | שרשרת אופציות מלאה | `data/current/` |
| `putvscall.csv` | Call vs Put לפי סטרייק | `data/current/` |
| `PROJECT_WORKPLAN.md` | הקובץ הזה | שורש הפרויקט |

---

## 10. מנגנון המרווח האופטימלי + מסחר-לפי-המלצות — ✅ הושלם (14/07/2026)

מעבר ל-6 האסטרטגיות ההיסטוריות, נבנה מנוע לבחירת רוחב ה-Short Iron Condor האופטימלי לפקיעה
קרובה, לצד שכבת "המנוע מול המציאות" ומסחר-דמו אוטומטי מבוקר.

### מנגנון המרווח (שלבים 1–6) — הושלם
- `src/payoff.py` — פרימיטיב P&L ל-condor (4 רגליים).
- `src/margin_calculator.py` — עקומת המרווח (גריד 1.0–3.0%): strikes אמיתיים, פרמיה, max_loss, P&L.
- `src/move_distribution.py` — התפלגות תנועות היסטורית + hold_probability + EV curve + conditioning.
- `src/margin_selector.py` — בחירת המרווח האופטימלי מהעקומה.
- `src/margin_validator.py` + `src/margin_backtest.py` — ולידציה + backtest היסטורי.
- `src/margin_recorder.py` — רישום יומי ל-`margin_recommendations` (Action, idempotent).

### שכבת "המנוע מול המציאות" (גשרים 1–2) — הושלם
- `decision_recorder.py` רושם החלטות מנוע; `decision_validator.py` מחשב hit-rate / regret מול ה-P&L האמיתי.
- **guard:** רק 6 האסטרטגיות המקוריות (strategy_id 1–6) נספרות; תיק ההמלצות (102) מוחרג —
  בצד Python וגם ב-Web (TS: `validationMath.ts` + השאילתות ב-`data.ts`).

### גשר 3 — מסחר-לפי-המלצות (דמו) — הושלם וחי ב-main
- `recommendation_trader.py` פותח Short Iron Condor דמו לפי ההמלצה האחרונה, מוגן ב: kill-switch
  (`RECO_TRADING_ENABLED`, **דלוק**), דדופ (עסקה אחת לפקיעה), שער מרחק-לפקיעה
  (`min_days_to_expiry=1` — מדלג על פקיעת אותו-יום; מחר עדיין נפתח), ו-strategy_id ייעודי 102.
- **תיק ההמלצות:** id=8 ("המלצות המערכת — Iron Condor"). 6 התיקים הקיימים (2–7) נותרו benchmark קבוע.
- מחובר ל-Action `auto_record_margins`. ריצה חיה אומתה: רישום→דדופ, ניסיון פתיחה→דילוג (כל הזכאיות כבר פתוחות).

### בדיקות
- **969** בדיקות pytest עוברות; **75** בדיקות vitest (Web) עוברות.
